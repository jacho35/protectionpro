# ProtectionPro — Backlog Features

## Open UX & Accessibility Findings (June 2026 audit)

Reconciled 2026-07-09 against `audit-2026-06-21.md` / `audit-responses.md`. These were accepted in the audit response but never implemented (the "UX overhaul" in `d7e095b` covered other items instead). Confirmed still open by reading current source, not just commit messages.

- ~~**Native dialogs (C2)**: ~41 `alert()`/`confirm()`/`prompt()` sites still in place instead of the app's styled modal system~~ — done: all sites now use the shared `UI` module (`ui.js`); former `alert()` notices became toasts, `confirm()`/`prompt()` became promise-based modal dialogs.
- ~~**Port-click silently starts a wire in Select mode (C4)**: `canvas.js:367-374`~~ — done: a Select-mode port drag now sets the wiring cursor and a status-bar hint, cleared on cancel.
- ~~**No canvas keyboard operation / no modal focus trap (C3)**~~ — done: `ModalFocus` (`ui.js`) traps and restores focus for all `.modal`s; `#sld-canvas` has `tabindex`/`role`/`aria-label` and arrow-key spatial navigation between components when keyboard-focused.
- ~~**`--text-muted` fails AA contrast (H14)**: `app.css:19`~~ — done: darkened to `#6d6d6d` (~4.9:1).
- ~~**No global analysis-busy indicator (H15)**~~ — done: `UI.setBusy` (ref-counted) shows a full-screen spinner/lock while any analysis is in flight, driven from `_setBusy`.
- ~~**No desktop toast system (H16)**~~ — done: shared `UI.toast` used on desktop; `MobileUI.showToast` delegates to it (single implementation).
- ~~**Non-General property sections collapsed by default (H21)**: `properties.js:96`~~ — done: default flipped to expanded (user collapse state still persists).
- ~~**Dark-mode result-table rows unstyled (H23)**: `symbols.css:225-243`~~ — done: `body.dark-mode` overrides for `.af-danger/.af-high/.af-medium/.af-low/.af-unknown` rows and `.af-warning-item`.
- **Not re-verified this pass** (still deprioritized as a UX-polish tranche per the original response): H17 (toolbar IA), H20 (branch-flow label overlap), H22 (export entry points scattered).

Already fixed despite audit claims (no action needed): C5 (undo-clear on revision restore, `revisions.js:286`), H18 (mobile properties panel is wired, `mobile.js:420`), H19 (annotation badge stacking has collision offsets), H24 (empty-state hint exists, `index.html:402`).

All Critical/High/Medium/Low findings from `audit-2026-07-09.md` are resolved, including **PROT-21** (gG fuse curve 0.1 s-gate re-fit) — see the Completed entry below.

## Arc Flash Enhancements
- ~~**Per-bus conductor gap / equipment class**: the arc-flash engine auto-selects the IEEE 1584 conductor gap from voltage only (LV fixed at 25 mm = MCC/panel, x = 1.641), so it cannot model LV *switchgear* (32 mm, x = 1.473) or non-typical gaps. Expose an equipment-class / gap field per bus so switchgear vs MCC/panel/cable is user-selectable. See `testing/case-arcflash-ieee1584/results.md`.~~ **Done** — see Completed.

## Protection Coordination Enhancements

## Grounding (IEEE 80) Enhancements
- ~~**Full IEEE 80 geometric factors**: the grounding engine simplifies three IEEE 80 quantities — `n = max(n_x, n_y)` (correct only for square grids; the standard uses `n = n_a·n_b·n_c·n_d`), `K_ii = 1.0` hardcoded (correct only for grids with rods; no-rods grids use `K_ii = 1/(2n)^(2/n)`), and `L_M = L_c + L_rod` (omits the Eq. 88 rod-length weighting `[1.55 + 1.22·L_r/√(L_x²+L_y²)]·L_R`). Net effect is a mesh (touch) voltage ~5–6 % high (conservative) for square-with-rods grids, and larger error for rectangular/L-shaped or no-rods grids. Implement the full formulas so mesh voltage and non-square / no-rods geometries match IEEE 80 exactly. See `testing/case-grounding-ieee80/results.md`.~~ **Done** — see Completed.

## Fault Analysis Enhancements
- ~~**Sequence impedance editor**: Positive, negative, zero sequence impedance entry per component~~
- **IEC 61363 marine/offshore fault analysis**: Fault analysis for isolated marine/offshore networks
- ~~**Static-load fault contribution guidance**: `static_load` contributes no short-circuit current (correct per its definition), but ETAP-style studies model lumped loads with a motor component as fault contributors. Surface this to users (tooltip/warning, or an optional "motor fraction" on `static_load`) so Case-3-type networks aren't silently under-predicted. See `testing/case-3-motor-lump/results.md`.~~ **Done** — see Completed.

## DC Analysis Enhancements
- ~~**Full IEC 61660-1 battery factors**: the DC short-circuit engine uses a simplified battery model (`i_p = E_B/R_BBr`, `I_k = 0.95·E_B/R_BBr`, `τ = L/R`) that omits the standard's refinements — the **0.9 factor on R_B** for the peak, **E_B = 1.05·U_nB** when the open-circuit EMF is unknown, the **+0.1·R_B** term in the I_k denominator, and the **T_B = 30 ms** battery time constant in the rise-time/`1/δ`. Fed raw nameplate inputs the engine reads ~5–12 % **low** (non-conservative) on the battery peak/quasi-steady-state currents. Apply these factors internally so nameplate inputs give the full-standard result. Verified: the core `E_B/R_BBr` + superposition reproduces the published IEC 61660 peak exactly when the preprocessed values are supplied. See `testing/case-dc-shortcircuit/results.md`.~~ **Done** — see Completed.

## Load Flow Enhancements
- ~~**PV-bus generator reactive display**: at a PV bus the on-canvas generator badge shows the machine's *scheduled* reactive (capped at its MVA rating), not the solver-computed PV reactive that actually holds the setpoint voltage. The solution is unaffected (voltage held, slack Q correct) but the badge understates/misreports Q. Report the solved PV Q on the badge. See `testing/case-loadflow-3bus/results.md`.~~ **Done** — see Completed.
- ~~**Source/generator load split**: Fix generator vs utility power split when utility is behind a source-connected transformer~~
- ~~**Fix array reduction error**: Fix load flow crash ("zero-size array to reduction operation maximum") when all buses are swing type~~
- **Harmonic load flow**: Frequency-domain analysis for non-linear loads

## TCC Chart Enhancements
- ~~**Mini SLD panel**: Vertical protection-path diagram alongside TCC chart showing IEC symbols, component names, ratings, and curve color accents — similar to ETAP STAR module~~
- ~~**Per-device TCC grading button**: Button in CB/fuse/relay properties panel to open focused TCC window showing only the selected device and its upstream protection for grading adjustment~~

## Multi-User & Collaboration
- **User authentication**: Login system with role-based access
- **Project sharing**: Share projects between users
- **Real-time collaboration**: Multiple users editing the same SLD simultaneously
- **Audit trail**: Track changes with user attribution
- ~~**Version history**: Track changes and revert to previous versions~~

## Reports & Export
- ~~**Detailed calculations report**: Multi-section PDF showing formulas, intermediate values, and step-by-step calculations for fault analysis (IEC 60909), load flow, arc flash (IEEE 1584-2018), cable sizing (IEC 60364), motor starting, duty check, load diversity, and grounding (IEEE 80)~~
- ~~**Project details tab**: Project metadata (project number, client, engineer, checked by, approved by, revision, date, description, company logo) in properties panel, used on PDF report cover pages~~
- **DXF/DWG export**: Export SLD to CAD formats

## Data & Integration
- **Plan Markup workspace** (in progress, `feature/plan-markup`): geographic markup module ported from Retic Builder Pro's Site Plan + Distribution Designer Pro's Floor Plan — import a site/floor plan (PDF/image), calibrate scale, place components + draw cable routes/trenches on it, push measured geometry into the Reticulation schedules, export CSV + DXF. This is the "site-plan geometry" deliberately scoped out of the Reticulation/ADMD module. See `PLAN_MARKUP_INTEGRATION_PLAN.md`. **Phase 0 done**: backend plan-image BLOB store (`PlanImage` model + `/api/plan-images` CRUD, SQLite FK enforcement so orphaned images null out on project delete) and vendored pdf.js 3.11.174. **Phase 1 done** (engine MVP): third "Plan" workspace tab + generalized `switchWorkspace` (SLD/Retic/Plan); dual-canvas engine (`plan-engine.js`) with centre-origin transform, rAF-coalesced draw, devicePixelRatio sizing, pan/wheel-zoom/two-finger pinch (native Pointer Events); declarative entity registry (`plan-defs.js`, retic domain + building starter set, discipline layers, styles); raster import → backend store with cache + orphan-claim on save (`plan-images.js`); tools (`plan-tools.js`) select/place/route/calibrate with grid(m)/element/vertex snapping; palette + registry-driven properties panel (`plan-ui.js`); local JSON-snapshot undo, keyboard shortcuts, dark-mode-aware canvas; `AppState.planMarkup` persisted through save/load (image bytes stay backend-side). Headless Playwright verified 16/16 (workspace switch, image upload round-trip, calibration, placement, undo/redo, persistence round-trip, 3-way tab isolation, no console errors). **Phase 2 done** (full toolset + PDF): trench/crossing/text/measure/crop tools; box/marquee multi-select with shift-toggle, tie-break click-cycling, and an on-canvas rotate handle; curved (Catmull-Rom) routes + route-finalize T-junction auto-split; PDF import via pdf.js (rasterize @scale 3 with 8192px cap, multi-page thumbnail modal with select-all/pick, original PDF retained for page re-render); discipline-layer emphasis panel; snap-toggle pills. Headless Playwright 12/12. **Phase 3 done** (v1 exports): one-way idempotent "Push to Schedules" (`plan-sync.js`) populates the Reticulation workspace from drawn geometry — adopts/creates minisubs & kiosks, walks LV feeders for fedFrom + feeder length/cable, service routes → erven with measured lengths, SL circuits → per-kiosk street-light kVA; reticId-first matching so rename/re-push never duplicates or clobbers user-entered load data. CSV schedules (`plan-csv.js`: elements/routes/trenches/crossings/street-lights, csvCell + BOM). AutoCAD R12 DXF (`plan-dxf.js`: metres, Y-flipped, discipline layers, POLYLINE/LINE/CIRCLE/TEXT, refuses when uncalibrated). Toolbar Push/CSV/DXF buttons. Headless Playwright 18/18 (calibrated 100 m route → feederLength 100 & CSV 100.00 m; TX–K1–K2 fedFrom chain; push idempotence + loadClass survival; SL phases; DXF structure/units/refusal). **Phase 4 done**: SL auto-path tool (draw a path → evenly-spaced poles + sl-route chain from the source kiosk, one undo step, metre spacing); background-plans panel (per-plan visibility/opacity/PDF page-nav/remove) + "Clean" button wired to `POST /api/plan-images/cleanup`. Headless Playwright 10/10 (pole generation + single-step undo, SL circuit sourcing, plans panel controls, cleanup endpoint). **Phase 5 done** (annotated export): `PlanEngine.renderExport` rasterizes the marked-up plan (background image + markup, honoring the crop box) into an offscreen canvas; `plan-export.js` downloads a PNG or an A3 jsPDF drawing sheet with a title-block strip (project/drawing no/revision/scale/date/drawn/client). **Phase 6 done** (building domain): `PLAN_DEFS` building set expanded to ~28 element types (power/lighting/small-power/switches/ELV & fire/control) + 9 building route types (feeders/circuits/containment/data/fire/DALI) + 6 building discipline layers; the palette + layer panel switch cleanly by domain, and the DXF writer maps the building layers. Headless Playwright 12/12 (building palette/layers/placement, renderExport pixels, PNG blob, PDF download, crop-honoring export). **Phases 7–11 done** (all previously-deferred features): **(7)** Bill-of-Quantities CSV (cable lengths × waste, equipment counts, excavation) with currency-neutral rates in settings; **(8)** multi-plan overlays — the engine applies each plan's offset/rotation/scale, plus a nudge-plan drag tool and 2-point registration (`align` tool solves a similarity from two feature→destination point pairs); **(9)** rooms/area zones (closed-polygon tool, area + perimeter, room CSV, drawn under markup, included in DXF/PNG/PDF); **(10)** lighting lux heatmap (`plan-lux.js` — first-order cosine + inverse-square point model from placed fittings, translucent heatmap overlay, peak-lux readout); **(11)** DXF import (`plan-dxf-import.js` — parses LINE/LWPOLYLINE/POLYLINE/CIRCLE/ARC/TEXT into a session reference underlay to trace over, not persisted to keep the project JSON small). Headless Playwright 13/13. **Plan Markup module feature-complete.** Backend suite still 127 pass. **Device symbols** (`plan-symbols.js`) replicate the source apps' glyph shapes — reticulation from Retic Builder Pro (kiosk = labelled filled box, minisub = diagonally-split transformer box, erf = ✕, pole/rmu/manhole = lettered discs) and building devices from Distribution Designer Pro's `_seDefaultElements` (socket = disc + stub, switch = disc + angled throw, DB = rectangle, isolator, light = disc + cross, two-winding transformer, data diamond, etc.), drawn at constant world size like the originals. Headless Playwright 8/8 (all glyphs render, visually distinct, per-type sizing, hit-test intact). **Model consolidation** (central-DB unification): all cable-carrying routes draw from the single central `STANDARD_CABLES` library (no per-module cable list); Main + Sub-Main feeders collapsed to one **Feeder** route; Main DB + Distribution Board collapsed to one **Distribution Board** element; load-time migration remaps legacy `bd_mdb`/`main_feeder`/`sub_main`. Headless Playwright 9/9. **Dynamic-block families** (AutoCAD-style): sockets, light fittings and switches are each a single palette entity whose sub-option properties re-render the permutation — Socket (gangs 1/2/3 + weatherproof), Light Fitting (kind: ceiling/downlight/batten/floodlight/wall/high-bay/emergency/exit), Switch (gangs 1/2/3 + kind: standard/2-way/intermediate/dimmer/PIR/key/timer/photocell). Glyphs computed parametrically in `plan-symbols.js` (ported from Distribution Designer's parametric logic); the separate variant types collapse into the families with a load-time props migration. Headless Playwright 13/13 (permutations render distinct, live variant switching resizes the glyph, legacy migration). **Specialised building cables** (`BUILDING_CABLES` in `plan-defs.js`): the building distribution routes select from Distribution Designer's specialised final-circuit/sub-main cable set (House Wiring T+E, Wiring H07V-R singles, Surfix, SWA multicore, DALI/0-10V control) — one library per cable class, filtered by category per route (final circuit → T+E/singles/Surfix; feeder → SWA multicore; DALI bus → control), while reticulation routes keep the central `STANDARD_CABLES` (kV-keyed) unpolluted. Headless Playwright 10/10. **Building ↔ SLD link** (building distribution does NOT touch the Reticulation schedule): a Distribution Board drawn in the Plan's building domain creates/links a `distribution_board` on the SLD (id-linked), and a feeder between two boards becomes a Cable/Feeder component wired MDB(out)→cable→SDB(in) carrying the drawn cable type + measured length (`distribution_board` gained an `out` port). The "Sync with SLD" action (the push button relabels by domain) reconciles both directions — Plan boards/feeders → SLD, and SLD cables joining two linked boards reflect back as Plan feeders — id-linked/idempotent, with DB rename propagating to the SLD. **Deletions propagate across views** on sync behind a confirmation ("Remove / Keep both"): deleting a board/feeder in either view removes its linked counterpart on the other (cascading — removing a board removes its feeders; removing a feeder removes its cable); "Keep both" re-establishes the link instead. Back-refs (`comp.planLink`) make deletions detectable. Headless Playwright 12/12 link + 6/6 deletion-propagation. **SLD-first workflow ("From SLD"):** when the network already exists on the SLD, the building palette lists unplaced SLD boards + supply (transformer/generator/utility/busbar, mapped to building symbols) under "From SLD"; clicking one arms a place-linked tool that drops a plan element bound to that existing SLD component (no duplicate). Placing both ends of an existing SLD cable auto-draws the plan feeder immediately (`reflectSldFeeders`). Headless Playwright 8/8. **DB→DB feeding via outgoing bus** (replaces the direct out-port wiring): a DB→DB feeder now adds a "Feeder to Sub-board" circuit type (`feeder_db` in `DB_LOAD_TYPES`) to the upstream DB's schedule and, on the SLD, creates one shared **outgoing bus below the DB** wired MDB(out)→bus(top)→cable→SDB(in); reverse/adopt reflection resolves the bus back to its owning DB; deletion + prune keep the bus and feeder ways consistent (empty bus removed, stale ways dropped). Headless Playwright 9/9. **Switchboard entity** (bus + CBs, multi-section): new `bus_duct` SLD component type (electrically transparent — added to the frontend + all backend TRANSPARENT_TYPES sets) + an adopt-only `bd_switchboard` plan element (`sldLinkTypes` maps `bus → bd_switchboard`). Adopting a bus in "From SLD" groups its whole switchboard — every bus section joined by a coupler CB or bus duct, plus the CBs/fuses/ducts on any section — into one plan element (`sldBuses`/`sldMembers`, `swLink` back-refs); the From-SLD list shows one entry per bus group. Reverse feeder reflection walks through board breakers (bus → CB → cable) to resolve a section bus to its switchboard; deletion propagation removes the whole group. Plan glyphs redesigned to convention: **DB = rectangle + diagonal**, **switchboard = feeder bays** (one bay per section). Backend 127 pass; headless Playwright 12/12 switchboard + all prior plan suites green. **Plan-first switchboard feeders**: the forward sync now creates SLD cables for feeders drawn from a switchboard (or DB) to a DB/switchboard — a switchboard feeds straight off its (section) bus, a DB via its shared outgoing bus (`_feederSourcePoint`/`_feederSinkPoint`); switchboard→switchboard and switchboard→DB both wire bus→cable→sink. Headless Playwright 7/7. **Cables follow moved components**: dragging or arrow-nudging an element re-snaps every attached route endpoint (fromId/toId + snappedTo vertices) to the element's new position live (`PlanMarkup.reconcileRoutes`), so feeders/services/SL routes stay connected. Headless Playwright 6/6. **Route node editing**: Shift+click a cable segment inserts a bend node; **double-click or right-click a node deletes it** (`PlanTools.deleteVertexAt` via `PlanEngine.findRouteVertexAt`), with the two endpoints protected (they're the cable's terminals) and full undo. Headless Playwright 6/6. **Fix**: the Plan workspace used `inset:0`, covering the toolbar (and workspace tabs) so there was no way back to the SLD — now anchored below the toolbar (top = live toolbar height, matching Reticulation), tabs stay visible/clickable. **Multi-floor buildings** (restores Distribution Designer's floor model, lost in the port): `planMarkup` now holds a `floors[]` stack — each floor a separate sheet with its own elements/routes/trenches/rooms/texts/measurements, **its own background plans and its own scale calibration**. The active floor's collections are mirrored onto `planMarkup.<key>` (the live working set the engine/tools read unchanged) via `switchFloor()` stash/hydrate; a **floor dropdown + ⚙ manager** (add/rename/reorder/delete, per-floor level & height, building-domain only) sits in the Plan toolbar. Building settings gain `floorHeight` + `riserFactor`; `planVerticalRunM(a,b)` sums the floor-to-floor heights between two storeys × the riser factor, and risers sharing a name across floors form a **vertical shaft** (`planRiserShaft`) — vertical runs appear as their own rows in the routes CSV and a "Riser cable — vertical" line in the BOQ. **SLD sync, CSV schedules, and BOQ aggregate across all floors** (each measured with its floor's own scale), so boards/feeders on any floor sync; DXF stays a single-sheet (active-floor) export, labelled with the floor. Legacy pre-floors projects migrate by wrapping the flat plan as a single "Ground" floor (`floors[]` was array-from-day-1, so no data migration); `toJSON` makes `floors[]` the single source and strips the live mirror. Headless Playwright verified: separate-sheet isolation, per-floor scale independence, vertical runs (7.7/3.85 m), riser shafts, save/load round-trip, legacy migration, floor switcher UI + manager, and cross-floor board→SLD sync — no console errors. **Auto-circuiting** (`plan-circuits.js`, ports Distribution Designer's floor-plan → final-circuit automation): load devices carry a circuit tag (`props.circuitDbId`+`circuitNo`); `syncLoads()` counts tagged lights/sockets/FCUs per way and writes each way's `load_va`/`plan_qty` into the linked SLD board schedule (per-way `_manualLoadOverride` respected, auto-named from the device mix) then recomputes the board load; `propagateFrom()` floods a tag along connected final-circuit routes; `bulkAssign()` distributes a board's connected untagged devices across ways with caps (10 lights / 6 sockets); `syncRoutedLengths()` writes routed circuit length into each way's `cable_m`. Properties-panel Circuit editor + a bd_db Auto-assign/Sync panel + a "⚡ Sync Circuits" building-domain toolbar action. Headless Playwright verified device VA, per-way load/qty, override protection, propagate, bulk-assign caps + grouping, routed length, and the panel/toolbar UI. Genuinely later/optional: photometric IES files, persisted DXF underlay via the backend store, cross-floor board↔board feeder cables auto-drawn through riser shafts (vertical run is already computed + costed; the SLD cable is not yet auto-generated for a riser-leg pair), per-fixture 3P+N tap-phase assignment + splice-JB joint-count. **Import legacy Distribution Designer `.json` projects** (`plan-dd-import.js`): the Plan toolbar Import now accepts a `.json` DD project (detected by `proj.buildings`) and rebuilds its buildings×floors as Plan Markup floors — origin shift (DD centres the image at the origin; we offset by imgW/2, imgH/2), FP_ELS/FP_ROUTE_TYPES → `bd_*`/route-type remap (clean 1:1 where possible, else nearest symbol with the original DD type stashed in `props._ddType`), per-floor scale carried verbatim, element ids regenerated with an old→new map so route endpoints stay linked, and embedded base64 rasters POSTed to `/api/plan-images` (geometry still imports if a floor's image can't upload). DD's DB/circuit electrical schedule + custom symbols are not imported (geometry + markup only). Multiple buildings flatten into `floors[]` (floor names prefixed with the building). Headless Playwright verified origin shift, scale/factor, light/switch/socket prop remaps, solar→bd_generator & submeter→bd_jb fallbacks (with `_ddType` stashed), route relinking + main→feeder, site-plan skip, non-DD-JSON rejection, and graceful image-upload failure.
- **Component library import**: Import manufacturer data (transformers, cables, etc.)
- ~~**NEC ampacity tables**: Add NEC Article 310 ampacity data alongside existing IEC 60364~~
- **Import from external tools**: Import from PSS/E, ETAP, or PowerWorld formats
- ~~**Cloud storage**: Project storage with user accounts and sharing~~ (file manager with folders implemented; user accounts pending)

---

## ETAP Feature Parity

Features identified by comparing ProtectionPro against ETAP's full module set.

### Analysis Modules

- ~~**Transient Stability Analysis**: Time-domain simulation of system disturbances — load shedding, fast bus transfer, critical clearing time, generator start-up with speed-torque dynamics~~ **Done (classical model)** — see Completed. Follow-up: two-axis (flux-decay) machine model + AVR/exciter + turbine-governor dynamics for voltage recovery and multi-swing damping.
- **Harmonic Analysis (full)**: THD calculation, harmonic current/voltage sources (VFD, UPS, converters), harmonic resonance identification, filter design; extends existing harmonic load flow backlog item
- **Frequency Scan**: Impedance vs. frequency sweep across the network for resonance identification
- ~~**DC Load Flow**: Load flow solver for DC buses (UPS, battery, telecom DC systems)~~
- ~~**DC Short Circuit**: Short circuit analysis for DC networks~~
- ~~**DC Arc Flash**: DC arc flash per Stokes & Oppenlander and DGUV-I 203-077 methods (AC arc flash already implemented)~~
- **Battery Sizing & Discharge Analysis**: Size batteries from duty cycle, model discharge curves, voltage performance over time
- **Optimal Power Flow (OPF)**: Economic dispatch, Volt/VAR optimization, switching optimization
- **Reliability Assessment**: SAIDI, SAIFI, MAIFI indices; failure mode & effect analysis (FMEA) for distribution networks
- **Voltage Stability Analysis**: P-V and Q-V curves, nose curves, voltage collapse prediction
- ~~**Unbalanced Load Flow**: Three-phase asymmetric network analysis for unbalanced distribution systems~~ ✓
- **Contingency Analysis (N-1 / N-2)**: Security analysis for single and double outage scenarios
- ~~**Motor Acceleration (Full Dynamic)**: Time-domain simulation with speed-torque curves, acceleration time, starter/contactor selection — beyond current locked-rotor static analysis~~ **Done** — see Completed.
- **Passive Filter Sizing**: Size LC/C harmonic filters to meet THD limits
- **Optimal Capacitor Placement**: VAR compensation placement and sizing optimization
- **Transformer Tap Optimization**: Automatic tap position optimization for voltage regulation
- **Flicker Analysis**: Voltage flicker assessment per IEC 61000-3-3 / IEC 61000-4-15
- **Feeder Hosting Capacity**: Nodal HC, Stochastic HC, and DER impact analysis for renewable integration planning
- ~~**Lightning Risk Assessment**: Structural and system lightning risk per IEC 62305-2~~

### Standards Coverage

- **ANSI/IEEE C37 Short Circuit**: Short circuit analysis per ANSI standards alongside existing IEC 60909 (required for US market)
- **AS/NZS 3000 Thermal & Shock Protection**: Australian/NZ wiring rules compliance checks
- ~~**SANS 10142 Wiring of Premises**: Automatic compliance checks for South African wiring rules~~

### Component & Modelling Gaps

- **Battery Energy Storage System (BESS)**: BESS component for SLD with charge/discharge modelling in load flow and dynamics
- **Variable Frequency Drive (VFD)**: Harmonic current source model for drives and converters
- **Autotransformer (2W & 3W)**: Autotransformer model with tap-changing voltage regulation
- ~~**UPS / Rectifier / Charger**: DC system source components for battery-backed systems~~ ✓
- **Static VAR Compensator (SVC) / STATCOM**: Reactive compensation FACTS devices for voltage control
- **HVDC Link**: DC interconnection between AC buses for HVDC system modelling

### Cable & Raceway

- ~~**Underground Raceway System (URS)**: Conduit fill analysis and thermal derating for bundled cables in conduit~~
- **Cable Pulling Analysis**: Tension, sidewall pressure, and jam ratio calculations for cable installation
- ~~**Standalone cable-sizing inputs**: cable sizing is network-integrated (current from load flow, fault from fault analysis, area inferred from R). Add an optional standalone mode with direct entry of design current, prospective Isc, and clearing time, plus a toggle between the IEC 60909-0 §12 thermal-equivalent Iₜₕ = Ik″·√(m+n) (default, conservative) and the bare-Isc adiabatic used in many hand-calcs. See `testing/case-cable-sizing-lv/results.md`.~~ **Done** — see Completed.

### Control & Schematic

- ~~**Control Circuit / Schematic Diagram**: AC/DC control circuit simulation — ladder logic, interlocking, panel wiring~~
- **Protective Device Sequence of Operation**: Simulate and verify the sequence of relay/CB operations during a fault event

### Integration & Interoperability

- **IEC CIM (Common Information Model) Import/Export**: Standard utility data interchange format
- **GIS Integration**: Geospatial mapping and overlay of the network diagram
- **Real-Time SCADA Integration**: Live data feeds for operational digital twin monitoring and control

### Platform

- **AI / Natural Language Search**: Query the model and run analyses using plain-language prompts

---

## Completed
- ~~DB circuit schedule bulk edit + way-number sort (`dbschedule.js`): each way row gained a selection checkbox (+ a header select-all with a tri-state indeterminate), and while ≥1 way is selected a bulk-edit bar appears — pick a field (Poles / Phase / Curve / Breaker / EL Group / Cable mm² / Length / Load VA / DF / Description), enter a value, and "Apply to selected" writes it to every checked way at once (poles↔phase kept consistent, 3P ways stay RWB, plan-driven ways get the same pin flags as a per-cell edit), plus "Delete selected". Selection is tracked by stable way `id` so it survives re-render/sort. Editing any way's number now re-sorts the schedule numerically (1, 2, 10 — not 1, 10, 2). Bulk Curve control defaults to C. Headless Playwright verified select-all, bulk curve/breaker apply-to-all, curve default C, numeric way-sort, and bulk delete — no console errors.~~
- ~~**Transient Stability Analysis (classical multi-machine, first pass)** — new time-domain rotor-angle study (`backend/analysis/transient_stability.py`, POST `/api/analysis/transient-stability`, Studies → Transient Stability). **Model** (Stevenson / Kundur ch. 13 classical): every synchronous generator is a constant voltage E′ behind its transient reactance X′d with inertia H; utility sources are infinite buses (very large H, fixed angle reference); all non-machine bus injections (loads, motors, inverter sources) are frozen as constant shunt admittances at their pre-fault operating point. Initial conditions from the positive-sequence load flow (E′, δ₀, P_m per machine). The network is **Kron-reduced to the machine internal nodes** each network state, giving P_ei = Σⱼ|Eᵢ||Eⱼ|(G_ij cosδ_ij + B_ij sinδ_ij); the swing equation dδ/dt = Δω, dΔω/dt = (ω_s/2H)(P_m − P_e − D·Δω/ω_s) is integrated with **RK4**. **Disturbances**: bolted three-phase bus fault cleared after a set time (faulted bus grounded during the fault, optional branch trip on clearing) with a **binary-search critical clearing time**; generator or branch trip; and load step. **Network reduction** reuses the new `network_reduction.build_branch_ybus` (extracted so the Thevenin builder, the multi-motor Zbus and this share one branch-assembly path). Instability = any machine's angle exceeding 180° from the centre of inertia. **Outputs**: stable/unstable verdict, CCT, per-machine rotor-angle δ(t) (relative to COI), frequency and electrical-power trajectories, and bus-voltage trajectories (recovered from the reduction), decimated ≤ 400 pts. **Frontend**: `transient.js` — a disturbance-setup modal (type-driven fields: fault bus + clear time + trip-on-clear + find-CCT; trip element; load + step %) and a results modal with a verdict banner, machine summary, and three signed-axis SVG charts (δ, frequency, bus voltage) with event markers, crosshair + all-series tooltip and a data table; new `generator` props **Inertia Constant H** and **Damping D** in a new collapsible "Transient Stability" property section (with FIELD_INFO ⓘ). **Validation**: the integrator + CCT search reproduce the single-machine-infinite-bus **equal-area closed-form CCT to 0.2%** (fault at the generator terminal ⇒ P_e = 0 during fault); stable below / unstable above the CCT; CCT falls with inertia. **Integration**: detailed calculations report section "6A. Transient Stability" (method, verdict, CCT, per-machine table, sampled rotor-angle trajectory) threaded through `/api/reports/calculations`; Study Manager gains a `transient_stability` study (default batch disturbance = 3-φ fault at the first generator's bus, 150 ms clear, CCT searched; honours a configured `stabilityDisturbance`), batch checkbox, result distributed to `AppState.stabilityResults`, persisted through save/load. 13 new backend tests (equal-area CCT anchor, stability threshold, inertia sensitivity, pre-fault equilibrium, all three disturbances, study-manager + PDF integration, edge cases); backend suite **205 pass**. Playwright end-to-end (build SMIB → config modal → fault run → verdict + CCT 623 ms matching theory + 3 charts, load-step field switching, zero console errors). Next: two-axis machine dynamics + AVR/governor. (`transient_stability.py`, `network_reduction.py`, `study_manager.py`, `pdf_reports.py`, `routes/analysis.py`, `routes/reports.py`, `schemas.py`, `transient.js`, `constants.js`, `properties.js`, `app.js`, `api.js`, `state.js`, `index.html`)~~
- ~~**Sequenced / coupled multi-motor starting** — the Dynamic Motor Starting study now simulates every motor on **one shared timeline** against a **multi-port Thevenin equivalent** of the network, so overlapping and staged starts sag each other's bus voltage (previously each motor was solved independently with the others frozen as steady loads). **Network reduction** (`backend/analysis/network_reduction.py`, new): builds the passive-network positive-sequence Ybus with sources as internal-impedance shunts (fault-engine primitives at c = 1.0, no IEC 60909 K_T — operating-point convention consistent with the load-flow V_pre) and inverts it to the bus-impedance submatrix **Z** over the motor terminal buses; mirrors `loadflow`'s branch assembly (series chains, transformer off-nominal π-model, solid bus links) and a source-stub walker with voltage-zone-correct cable bases. Diagonal Z_ii reproduces the old single-bus `_thevenin_at_bus` on radial networks (validated); off-diagonals add the cross-bus coupling. **Solver** (`dynamic_motor_starting.py` rewrite): because a machine's admittance Y(s) is voltage-independent, each RK2 half-step is a single linear complex solve (I + Z·D)·V = V_pre with D the base-converted motor load-admittance diagonal (soft-starter current limit resolved by a short fixed-point on α); one baseline load flow (all staged motors off, "running" motors kept as demand) gives V_pre. Two new per-motor props: **Sequence Role** (`dyn_role`: *starts* / *already running* — the latter is a steady background load, not a simulated start, reproducing the legacy model on demand) and **Start Time** (`start_time_s`, when the motor energises on the shared timeline); both on induction + synchronous motors with FIELD_INFO ⓘ. **Frontend** (`dynmotor.js`): a "Start sequence — shared timeline" header with role/timing chips and a bus-voltage-vs-global-time overview chart; per-motor charts gain energise + changeover markers on the global timeline (generalized `markers` array); acceleration time is reported relative to energisation; results carry a new `sequence` payload. Backward-compatible: a single motor at start_time 0 reproduces the old divider numbers exactly (all 23 prior tests pass). +4 regression tests (two-motors-deeper-dip, staggered energise + overview dip, running-role loads-but-not-simulated, cross-bus coupling); backend suite **192 pass**. Zbus diagonal validated against `_thevenin_at_bus` across radial/stub/shared-source topologies; Playwright end-to-end (build 2-motor network → staggered run → modal overview + schedule + tabs, property fields render/commit, zero console errors). (`network_reduction.py`, `dynamic_motor_starting.py`, `constants.js`, `dynmotor.js`, `test_dynamic_motor_starting.py`)~~
- ~~Fix Distribution Board circuit schedules sharing one array: `AppState.addComponent` copied `def.defaults` with a shallow spread (`{ ...def.defaults }`), so the nested `circuits: []` (and `el_ratings`) were shared **by reference** — every distribution board created in a session pointed at the *same* physical array (and at the global default in `constants.js`), so adding/editing a way on one board mutated all boards and polluted the default that fresh boards inherit. This read to the user as "adding a new entry overwrites all the existing entries." `pasteClipboard` had the identical shallow-copy flaw. Both now deep-clone (`structuredClone(def.defaults)` / `structuredClone(comp.props)`) so each board owns its schedule. Headless Playwright before/after: pre-fix two new boards shared one array (push to DB1 → DB2 and the global default both gained the way); post-fix boards and pastes are fully independent — no console errors. (`state.js`)~~
- ~~Route Distribution Designer exports through File ▸ Import JSON: a DD project (`proj.buildings`, imports into the Plan workspace via `PlanDdImport`) is not a ProtectionPro SLD project, so the main importer's new shape validation rejected it as "not a ProtectionPro project — missing a components array" with no path forward. `importFromFile` (`project.js`) now detects `PlanDdImport.isDdProject(data)` after `JSON.parse`, shows an info toast, switches to the Plan workspace (`switchWorkspace('plan')`) and hands off to `PlanDdImport.importFile(file)` — so a user who reaches for the familiar File import gets their buildings/floors imported instead of a dead-end error. Verified end-to-end against the live stack on a real 115 MB DD export (E5435): detected, routed, and imported 6 floors / 544 devices / 563 routes / 9 plan images with no errors; and the routing branch reproduced headlessly on the edited build. (`project.js`)~~
- ~~Fix silent JSON-import failure: `AppState.fromJSON` did no shape validation (every field was `data.X || default`), so a syntactically valid but non-ProtectionPro file ({}, [], `null`, another app's export) reset the canvas to empty and reported "Project imported from file." with no error. Added `AppState._validateProjectData(data)` — rejects non-objects, a missing/mistyped `components` array, a mistyped `wires` array, and per-item garbage (components need id+type, wires need an id) with a specific message — called at the top of `fromJSON` **before** `reset()`, so a bad file now throws a clear error and leaves the current project untouched. `importFromFile` (`project.js`) separates the `JSON.parse` catch ("Not a valid JSON file") from the shape catch ("Invalid project file"), adds a `reader.onerror` handler for read failures, and replaces the blanket success text with a component/wire count toast so a legitimately empty import is visible. Headless Playwright: 7 bad payloads all rejected with current project preserved, valid + empty-valid projects still import, no console errors. (`state.js`, `project.js`)~~
- ~~LV earthing-system types (TN-S / TN-C / TN-C-S / TT / IT) as a first-class property. Every LV source (a transformer with an LV secondary ≤1 kV, or a utility supplying ≤1 kV) gains an **Earthing System** select plus TT/IT earth-electrode resistances **Source Earth R_B** and **Installation Earth R_A** (`constants.js` transformer + utility defaults/fields, with a new numeric `showWhen: {max}` predicate in `properties.js` so the field only appears for LV sources; help text for all six new keys). The **fault engine** models them per IEC 60364-1: `fault.py::_transformer_zero_seq` adds the soil earth-return `3·(R_A+R_B)` into the LV zero-sequence loop for **TT** (so Ik1 collapses while Ik3 is untouched) and blocks the first-fault path for **IT** (Ik1 ≈ 0); TN-* keep today's metallic return, and MV/HV faults + legacy field-absent projects are byte-identical (defaults to TN-S). `_applyVectorGroupGrounding` keeps the system consistent with the neutral (ungrounded LV ⇒ IT). **Compliance** (`compliance.js`) gains a new SANS 10142-1 / IEC 60364-1 §312 earthing-system check per LV source — TN-S pass, TN-C-S PME (RCD downstream of the N/PE split), TN-C (RCD forbidden on the PEN — fail if a residual device is present), TT (RCD mandatory + `R_A·IΔn ≤ 50 V`, reusing the DB-schedule EL-group IΔn), IT (insulation-monitoring warning) — the existing `10·In` earth-fault-disconnection check now defers to it for non-TN systems, and the transformer-neutral check reports the declared system (IT no longer flagged as a fault). 6 new standards-anchored regression tests in `test_regression.py` (TT collapse + monotonicity in R_A, IT zero first-fault, TN-variant identity, MV-ignored, field-absent == TN-S); backend suite 188 pass. (`constants.js`, `properties.js`, `fault.py`, `compliance.js`, `test_regression.py`)~~
- ~~In-app Verification & Validation report — a standalone, self-contained `frontend/verification.html` presents all 15 standards-anchored V&V examples as worked cases: each shows the governing standard, a clickable source (powerprojectsindia / ESE 470 / CED E03-035 / IEEE & IEC standards), the network/inputs, and a reference-vs-ProtectionPro-vs-error results table, with PASS/Qualified badges and a top scorecard (12/12 engines, 16/16 SC points, 0.13 % worst-case, ±2 % tolerance). Grouped: Short Circuit (IEC 60909 ×4), Load Flow (NR + unbalanced), Protection & Ratings (arc flash, cable sizing, duty check), Motor/Grounding/Load (motor starting, IEEE 80, diversity), DC systems (load flow, IEC 61660-1 short circuit, DC arc flash). The page reuses the app's CSS-variable palette and honours the app's dark-mode preference (reads `localStorage['protectionpro-dark-mode']`, falls back to `prefers-color-scheme`) with a no-FOUC head script; print-friendly. Reached from Help → new **Verification** tab, which carries a summary scorecard table and a prominent "Open the full Verification report" link (`verification.html`, new tab). Headless Playwright: Help tab shows and switches, link resolves; standalone page renders 15 case cards, 15 scorecard rows, working source links and in-page anchors, correct light (`#f5f6f8`) and dark (`#252536`) backgrounds — no console errors. (`frontend/verification.html`, `index.html`)~~
- ~~Verification / Standards template library — the 15 standards-anchored V&V cases from `testing/` are now loadable in-app. A new generated module `frontend/js/verification-templates.js` (`VerificationTemplates`) embeds each `testing/case-*/project.json` verbatim, stamped `dataVersion: 2` so `AppState.fromJSON` skips the pre-v2 cable-resistance migration that would otherwise rescale the raw article `r_per_km` values (verified: SC Case 2 cable stays 9.8 Ω/km through a real `fromJSON`). `templates.js` merges `VerificationTemplates.meta` into the picker under a new "Verification / Standards" category (`show()`) and `generate()` returns a `structuredClone` of the embedded project. Covers all 15: SC Cases 1–3 + 220/33 kV (IEC 60909), LV cable sizing (IEC 60364), 3-bus load flow (Glover/NR), arc flash (IEEE 1584-2002), grounding (IEEE 80), motor starting, DC load flow, DC short circuit (IEC 61660-1), duty check, load diversity, DC arc flash, unbalanced load flow. Regenerate with the build script if the case files change. Headless Playwright: the category shows 15 cards, "SC Case 1 — No Load" loads (4 components/3 wires, renders on canvas), no console errors. (`frontend/js/verification-templates.js`, `templates.js`, `index.html`)~~
- ~~On-diagram indicator for under-rated devices. A small red warning triangle (with a tooltip listing the reasons) now marks under-rated protective devices on the SLD, so you don't have to open the Compliance report to spot them. `Compliance.deviceRatingFlags()` returns `Map(compId → {level, reasons})` from cheap nameplate/topology checks — rated voltage below the connected bus, and a dedicated-motor device rated below the motor FLC — plus breaking capacity below the fault level when fault results exist (reuses `_findConnectedDevices`/`_motorFLC`/`_motorProtectiveDevices`). `Annotations.render` draws a `.underrated-marker` at each flagged component (cached during a component drag so positions move without recomputing). A "Under-rated Device Flags" toggle in the View menu (`AppState.showRatingFlags`, default on) shows/hides them. Headless Playwright: a 32 A CB on a 22 kW motor (FLC 40.2 A) gets a marker tooltipped "Rated 32 A below motor IM FLC (40.2 A)"; toggling hides/shows it — no console errors. (`compliance.js`, `annotations.js`, `state.js`, `app.js`, `index.html`)~~
- ~~Fault-at-terminal is now also a right-click canvas action. `ContextMenu.openForComponent` adds a "Fault at Terminal" item for induction/synchronous motors and static loads (parallel to the bus "Run Fault Analysis Here"), reusing `Properties.faultAtTerminal`. Headless full-stack Playwright: right-clicking a motor shows the item and clicking it faults the terminal node ("M9 terminal", 3.06 kA) — no console errors. (`contextmenu.js`)~~
- ~~Core-balance CT (CBCT) support — part 2 (TCC coordination for the MCCB integral earth-fault trip). An MCCB/ACB with an earth-fault shunt-trip configured (`ef_trip_ct` + `ef_pickup_a`) now plots in the TCC: `_loadDevicesFromNetwork` pushes a synthetic definite-time earth-fault device (`deviceType:'relay'`, `relayType:'50N/51N'`, `curveName:'Definite Time'`, `pickup=ef_pickup_a`, `tds=ef_delay_s`, `id=<cb>__ef`, `efParent`), reusing the relay curve/trip-time/grading machinery. `_deviceElementClass` returns `earth`, so it grades at the SLG current like any 50N/51N and stays out of 3-phase grading. Its CT-saturation comes from the associated CBCT. Editing its pickup/delay in the TCC settings syncs back to the breaker's `ef_pickup_a`/`ef_delay_s` (new `efParent` branch in `_applySelectedDeviceSetting`). Headless Playwright: the element loads as "MCCB1 E/F" (50N/51N, Definite Time), earth class, trips 0.3 s above the 100 A pickup / never below, and settings edits round-trip to the CB (60 A / 0.5 s) — curve draws, no console errors. Completes the CBCT feature: a core-balance CT now drives either a 50N/51N relay or an MCCB's integral shunt-trip, both coordinating on the TCC. (`tcc.js`)~~
- ~~Fault-at-terminal placement for loads/motors. A "Fault at Terminal" action button on induction/synchronous motors and static loads runs fault analysis at that component's terminal — an end-of-feeder fault. `Properties._terminalBusId` walks transparent devices to the load's own bus, or falls back to the auto-inserted terminal node (`__term__<id>`) for a load wired behind a cable/transformer; `faultAtTerminal` posts that as the `fault_bus_id`, enables the fault result boxes, and renders. The synthetic terminal is stripped from load flow but kept in fault results, so the badge anchors on the load ("… terminal") and — because these terminals land in `faultResults.buses` — the existing TCC "Verify Sequence" now covers end-of-feeder faults too. Rebound in the mobile properties-sheet mirror. Headless full-stack Playwright: a motor directly on a bus faults that bus (49.96 kA); a motor behind a cable faults its terminal node "M9 terminal" (3.06 kA, reflecting the cable Z) — no console errors. (`properties.js`, `mobile.js`)~~
- ~~Core-balance CT (CBCT) support — part 1 (device + earth-fault shunt-trip data model). The CT component gained a `ct_type` field (`phase` | `core_balance`); a core-balance CT is a window/toroidal CT that encircles all phases and measures residual (earth-fault) current, drawn with a distinct double-ring symbol (`symbols.js` `ct()` branches on `ct_type`; `.symbol-cbct` inherits the CT stroke). It feeds the existing `50N/51N` relay as its measuring CT — which the TCC already grades at the SLG current — so the relay-based sensitive-earth-fault scheme works end-to-end. For the relay-less scheme (a CBCT tripping an MCCB's shunt-trip release directly), the CB gained integral earth-fault fields shown for MCCB/ACB: `ef_trip_ct` (the CBCT), `ef_pickup_a` (residual pickup), `ef_delay_s` (definite-time delay), with FIELD_INFO. Headless Playwright: phase CT = 1 circle, core-balance = 2-circle ring updating live on `ct_type` change; MCCB shows the three E/F fields with the CBCT in the picker, hidden when switched to MCB — no console errors. Part 2 (TCC coordination curve for the MCCB integral earth-fault element) still to come. (`constants.js`, `symbols.js`)~~
- ~~TCC: added an IEC 60898 curve selector (B/C/D) to the device-settings panel for MCBs. The panel previously exposed only Type / Trip Rating / thermal + magnetic pickup / delays, so the curve — shown in the device-list label ("MCB 32A curve B") — couldn't be seen or changed there. `_renderSelectedDeviceSettings` now renders a curve dropdown (`data-sel-field="cb.mcb_curve"`) visible only for MCBs; `_applySelectedDeviceSetting` sets the magnetic pickup from the curve (B 5×, C 10×, D 20× In via `MCB_CURVE_MAGNETIC`, mirroring the properties panel) and updates the field live, toggles the curve row when the CB type changes (defaulting a freshly-typed MCB to curve C), and syncs `mcb_curve`/`magnetic_pickup` to the SLD component. Headless Playwright: curve shows for the Kalulushi MCB (B), changing B→D updates magnetic pickup 5→20 and the list label, syncs to the component, and the row hides when switched to MCCB — no console errors. Frontend-only (`js/tcc.js`).~~
- ~~Mobile: the properties sheet now re-syncs live when a change re-renders the desktop panel. The sheet is a one-time innerHTML clone of `#properties-content`; a field change forwards to the matching desktop input and fires `change`, and for a standard-device / unit select the desktop `onFieldChange` calls `Properties.show()` (rebuilds the panel). The clone was never refreshed, so auto-filled/locked values (e.g. picking a standard CB updating trip + curve) only appeared after closing and reopening the sheet. Extracted the mirror+rebind into `MobileUI._mirrorPropertiesToSheet(compId)`; the per-input change forwarder now detects the rebuild (the forwarded desktop node becomes `!isConnected`) and re-mirrors, so the sheet reflects the new values immediately. Headless Playwright (390 px viewport): changing the standard CB in the sheet updates the locked trip/curve/rating fields live — no close/reopen — with no console errors.~~
- ~~Properties: fields populated by a standard-device selection are now locked (greyed/read-only) while that standard is selected — pick "-- Custom --" to edit them. Previously selecting a standard CB filled the trip rating, curve, pickups etc. but left them editable, so it was unclear they were driven by the selection and edits silently diverged from the chosen device. `renderField` now disables the library-controlled fields (`Properties._libraryControlledFields` / `_lockedByStandard`) for **cb** (cb_type, mcb_curve, trip_rating_a, rated_current_a, rated_voltage_kv, breaking_capacity_ka, thermal/magnetic pickup, delays), **transformer** (rated_mva, voltages, z_percent, x_r_ratio, vector_group) and **fuse** (fuse_type, rated_current_a, rated_voltage_kv, breaking_capacity_ka); `state` and other non-library fields stay editable, and the standard selector itself stays editable. Cable is intentionally excluded (keeps its edit-freely + reset-to-default UX). Disabled fields are greyed opacity-based so it reads in both themes (`css/properties.css`). Headless Playwright verified CB fields LOCKED with a standard and editable on Custom, transformer likewise, cable unaffected — no console errors.~~
- ~~Dark mode: compliance report status rows were unreadable. `.compliance-row.compliance-{fail,warn,pass} td` hardcoded light pastel backgrounds (#fff5f5 / #fffcf0 / #f6fff6) while the row text comes from the theme's `--text-primary`, which is near-white in dark mode → light text on a near-white background. Added `body.dark-mode` overrides with dark status tints (#3a1f24 / #3a2f1d / #1f321f) and swapped the table cell border `#eee → var(--border-color)`. Headless Playwright confirms WCAG contrast now ~10–11 in dark mode and unchanged (~16) in light. Frontend-only (`css/app.css`).~~
- ~~Compliance now flags an under-rated motor protective device from nameplate data, independent of load flow. The existing "Protection Device Ratings" check compared a CB/fuse's rated current against the *load-flow branch current* through the adjacent cable — so it (a) only ran when load flow results were present and (b) silently went quiet whenever an edit cleared results (`AppState.clearResults()` on any property change). Result: a 32 A CB feeding a 22 kW motor (FLC ≈ 40 A) showed no flag right after you set the rating. New nameplate check `Compliance._checkMotorCircuits()` (`frontend/js/compliance.js`, section "Motor Circuit Protection", IEC 60947-4-1): for a device that dedicatedly protects a **single** motor (`_motorProtectiveDevices` boundary walk — cb/fuse are boundaries, buses/cables/switchgear traversed, other loads abort so diversified feeders are skipped and can't false-positive), it FAILs when rated current < motor FLC (`_motorFLC`, matching the backend convention) and WARNs when the starting current (`_motorStartMultiple` — DOL/star-delta ⅓/autotransformer 0.64/soft-starter limit/VFD) exceeds the breaker's magnetic trip (`_deviceMagneticTripA` — MCB curve B 5×/C 10×/D 20× In, else instantaneous pickup). Runs with zero analyses. Headless Playwright verified with `hasLoadFlow=false`: Kalulushi's CB5 → FAIL (32 A < 40.2 A) and no false starting warn (star-delta inrush 80 A < 160 A Type-B trip); a 63 A breaker → PASS only; a 45 A/DOL breaker → PASS on FLC + WARN on starting (241 A > 225 A). Note the load-flow-based check in "Protection Device Ratings" still also fires when load flow is present (complementary basis).~~
- ~~Fault analysis now reports a fault level at a motor/load terminal wired behind a cable/transformer with no busbar of its own — the same auto-inserted terminal node as load flow (`insert_implicit_load_buses`), but **kept in the fault results** (not stripped) since the terminal fault level is the useful output. `run_fault_analysis` inserts the nodes up front; a single-bus fault (a real `fault_bus_id`) never computes them (the bus filter drops them). NB: fault analysis was **not** wrong before — its DFS source-path walker already traversed cables and counted the motor's infeed at the real buses (verified: real-bus levels are byte-identical with/without the terminal node); the only gap was the missing terminal result, which previously required drawing a bus. Frontend renders the terminal fault badge anchored on the load (`AppState.isSyntheticBus`/`syntheticBusLoadId`/`resultBusComponent`; `annotations.js` fault + voltage-depression loops), and the fault name-fallbacks in `reports.js`/`project.js`/`tcc.js` gained an `r.bus_name` step so the friendly "M1 terminal" name shows instead of the raw `__term__…` id (backend PDF/CSV already name via `bus_name`). Load-flow results still strip the synthetic nodes (unchanged). Verified: 1 new regression test (terminal fault ≡ manual-bus reference, real buses unchanged, name = "M1 terminal"; suite 182 pass) + full-stack Playwright (real fault run returns the terminal node at 3.93 kA and the canvas renders a draggable "FAULT — M1 terminal" badge on the motor, no console errors).~~
- ~~Load behind a cable/transformer with no terminal busbar was silently dropped from load flow (and every bus-based study). Loads are gathered by walking out from each bus through *transparent* devices only (CB/switch/fuse) — a cable is not transparent, so a motor/static-load/cap-bank wired `bus → cable → load` (no bus at the load terminal) was never attached to any bus, and the feeding cable, reaching only one bus, was dropped as a branch too. Net effect: the load vanished, the bus solved at 1.0 pu, and **motor-starting reported 0% volt drop / "pass"** (`_find_motor_bus` returned None → the 1.0 pu terminal default). The manual workaround was to draw a bus between the motor and its cable. Fix (`backend/analysis/loadflow.py`): `insert_implicit_load_buses()` synthesises exactly that node — an idempotent pre-pass that gives every dangling load (`motor_induction`/`motor_synchronous`/`static_load`/`capacitor_bank`, AC only) a terminal bus wired `load → synthetic bus → cable → …`, skipping loads that already reach a bus. `run_load_flow` runs it at entry and, by default, collapses the synthetic buses out of the public result (drops their bus rows, re-points branch endpoints back to the real load id) so they never reach the UI/reports/CSV; `run_motor_starting` calls it too and reads the terminal voltage with `include_synthetic=True`. All engines built on `run_load_flow` (motor-starting, cable-sizing, duty-check, study-manager, dynamic-motor) inherit the fix. Verified: synthetic reproduction shows the no-bus topology now gives an identical motor terminal voltage (0.6264 pu) and dip (33.7%) to the manual-bus reference, with no synthetic leak into public buses; 2 new regression tests (motor-behind-cable ≡ manual-bus; static-load-behind-cable becomes a current-carrying branch) + full suite 181 pass.~~
- ~~Mobile: component result labels are now draggable via long-press. Component analysis results (load-flow branch flows, cable size/length, etc.) render as `comp-data-label`/`comp-name-label` text — not as the draggable bus annotation badges — and their drag was hard-gated to mouse only (`e.pointerType === 'touch' ? null`), so on a phone they couldn't be repositioned at all (bus result badges could, being real `.draggable-annotation` groups). Added touch long-press promotion in `canvas.js`: a touch on a label arms a 450 ms hold (`_armLabelLongPress`/`_promoteLabelLongPress`/`_clearLabelLongPress`); holding still grabs the label (haptic `navigator.vibrate` cue, cancels the pan/tap the touch had begun, hands off to the existing `labelDrag`/`nameLabelDrag` machinery), while a quick touch or a drag-before-hold falls through to pan/deselect unchanged. Desktop mouse path (immediate drag) is untouched. Headless Playwright verified quick-drag = no move, 450 ms hold activates `labelDrag`, hold-then-drag moves the label by the expected delta, and desktop mouse drag still grabs on mousedown — no console errors.~~
- ~~Plan Markup — switch & socket symbols/options corrected to convention. **Switches**: the Type is 1-way / 2-way / intermediate (+ dimmer/PIR/key/timer/photocell function variants), drawn as **1-way = open circle, 2-way = filled circle, intermediate = filled circle with a line above** (was a filled circle with a "2"/"X" label). **Sockets**: replaced the wrong `gangs` (1/2/3) with **Outlets = Single / Double / Double + USB** — glyph is a circle-on-diameter with one prong (single) or two (double), a "USB" label for double+USB; load follows suit (single 200 / double 400 / double+USB 500 VA). Legacy `gangs` sockets migrate on load (`1`→single, else→double); the DXF block-variant key + round-trip import updated to match. Verified by render + node syntax.~~
- ~~Plan Markup DXF round-trip fidelity (ezdxf, backend-built AC1015): DXF export/import moved off the browser onto the backend (`backend/analysis/plan_dxf.py` + `routes/plan_dxf.py`, `ezdxf==1.4.4`) so the file is guaranteed spec-valid. **Export** writes a real `BLOCKS` section — one block per symbol variant whose geometry is translated from the on-screen `PLAN_SYMBOLS` recipe (so shapes export exactly as drawn) — an `INSERT` per device with `ATTRIB`s (Name/Type/Board/Circuit/Phase/Load/Cable), curved circuits as **true SPLINE** (fit points), straights as `LWPOLYLINE`, discipline layers with true-colour, and a `PP_META` block carrying the scale factor. Coordinates are world0-relative Y-flipped (`X=x·f, Y=-y·f`) so the inverse is exact. **Import** recognises our own files (PP_META + PP_* blocks) and rebuilds native, editable devices/routes/rooms with their attributes — relinking each device's circuit to its board by name and restoring the symbol variant from the block name — while third-party DXFs are exploded (blocks flattened) into the grey trace-over underlay as before. The frontend (`plan-dxf.js`/`plan-dxf-import.js`) are now thin clients that gather the payload / POST the file. Verified: ezdxf `audit()` clean (0 errors/fixes), attributes + spline fit-points + meta exact on read-back, foreign-DXF underlay flatten, backend suite 176 pass, and a full **UI round-trip on the deployed stack** (export → clear → re-import rebuilt the DB + 3 lights with variant=downlight, phase=3P, circuit relinked, and the curved spline route — no console errors). Curved routes: `circuit`/`lighting_ckt` runs now default to splines (`PlanTools.curvedByDefault`) with the per-route "Curved" checkbox as the straight-polyline toggle.~~
- ~~Plan Markup — fix the DB symbol: the `bd_db` glyph rendered as a faint box dominated by a diagonal + tiny offset "DB" (unclear). Redrawn as an enclosure card — incomer stub, "DB" header + divider, and three way rows — matching the SLD `distribution_board` symbol so the plan and SLD read the same. Verified by render.~~
- ~~Plan Markup — edit a board's circuit schedule from the plan: selecting a `bd_db` in the Plan workspace now offers "📋 Edit Circuit Schedule", which opens the **same `DBSchedule` modal the SLD uses** for that board (`DBSchedule.open(el.sldId)`) — full ways/breakers/phases/loads editor, not a plan-only summary. If the board isn't on the SLD yet it prompts to Sync first; `DBSchedule.close()` refreshes the plan board panel so the way count reflects edits. Headless Playwright verified the button, the modal opening titled for the board with its ways listed, and a clean Escape-close with no console errors.~~
- ~~Plan Markup per-device circuit attributes: every circuit-connected building device now carries editable **Load (VA)**, **Board (DB)**, **Circuit no. (way)** and **Phase (1P/3P)** attributes in the properties panel — extending the auto-circuiting tags. The Circuit editor shows on any consumer (`PlanCircuits.isCircuitDevice` = building `bd_*` except infrastructure — utility/transformer/generator/DB/switchboard/riser/JB); Load is an override (blank = auto from watts/gangs/type, shown as the placeholder), Phase is single/three-phase. `syncLoads()` now folds all tagged devices (not just lights/sockets) into the linked SLD way, and derives each way's `poles`/`phase` from its devices — a 3P device makes the way three-phase (R/W/B), all-1P keeps the assigned phase. Changing any of the four re-syncs the board schedule live. Headless Playwright verified the editor gating (consumers vs infra), a 3P override way (2×2500 → 5000 VA, poles 3P/RWB), a 1P light way, live 1P→3P flip, and the switch panel rendering all four fields — no console errors.~~
- ~~Plan Markup array + path device placement (ports Distribution Designer's "Light Array" + adds along-a-path placement): a placement-mode segmented control (Single / Array / Path) in the building palette re-arms the clicked device onto one of three tools. **Array** (`array` tool) — drag a rectangle, enter columns × rows, and it drops a grid of the device (edges inclusive, grid-snapped). **Path** (`devpath` tool) — draw a polyline (click points, Enter), enter a count or `Nm` metre-spacing, and it lays the device evenly along it (lights along a run, sockets along a wall…) and wires them with one final-circuit route (`lighting_ckt` for lights, else `circuit`) so routed length flows through. Both **auto-tag to a circuit**: `PlanCircuits.autoTagDevices()` assigns the batch to the nearest board's next free way(s), chunked by the per-type cap (10 lights / 6 sockets), and materialises the loads. Headless Playwright verified a 4×3 array (12 lights → 2 ways, loads on the DB), a 6-socket path (evenly spaced, wired first→last, tagged), and the mode selector — no console errors.~~
- ~~Plan / Distribution workspace on mobile: it was previously desktop-only — its Plan/Reticulation tabs live in `#toolbar` (hidden on phones by `mobile.css`), so the workspace was unreachable, and its two fixed-width side panels (260 + 300 px) crushed the canvas. Fixes: (1) **Reachability** — a Workspace switch (SLD / Reticulation / Plan) added to the mobile "More" sheet, wired in `mobile.js` to `window.switchWorkspace`; the mobile header + bottom nav are siblings of `#app-container` so they survive the switch. (2) **Chrome** — `app.js switchWorkspace` toggles `body.mobile-ws-secondary`, under which `mobile.css` hides the SLD-only FABs / selection bar / Components+Analysis nav buttons and fits `#plan-workspace`/`#retic-workspace` between the mobile header (top 52 px) and bottom nav. (3) **Layout** — on ≤768 px the plan toolbar collapses to a compact primary row (☰ Parts · ✎ Props · Select · Fit · ⋯ More); the ~18 remaining controls (Calibrate/SL Path/Crop, floor switcher + manager, Import, Lux, snap toggles + grid, Push/Sync Circuits/CSV/DXF/PNG/PDF, scale readout) live in a grouped, touch-friendly **⋯ overflow dropdown** (`#plan-tb-overflow`, `display:contents` on desktop so the layout is unchanged there, a dropdown panel on mobile) — replacing the previous 1723-px horizontally-scrolling strip. The canvas goes full-width, and the palette + properties panels become slide-in drawers toggled by the Parts/Props buttons (`plan.js` `_toggleDrawer`/`_toggleOverflow`), with a shared tap-catching backdrop; arming a place/route tool auto-closes the palette, tapping an item auto-opens Properties, and picking any overflow action closes the dropdown. Touch drawing already worked (native Pointer Events + pinch). Headless Playwright on a 390-px viewport verified reachability, workspace fit (top 52 px, canvas 390 px), SLD-chrome hidden, drawer open/backdrop-close, and clean return to SLD restoring the chrome — no console errors.~~
- ~~Dynamic Motor Starting (time-domain acceleration study) — `backend/analysis/dynamic_motor_starting.py`, POST `/api/analysis/dynamic-motor-starting`, Studies → Dynamic Motor Starting. **Motor model**: single-cage equivalent circuit with shunt magnetizing branch (I_mag = 0.3 pu) and linear deep-bar slip-dependent rotor resistance R₂(s) = R₂ᵣᵤₙ + (R₂ₛₜₐᵣₜ − R₂ᵣᵤₙ)·s, fitted per IEEE 3002.7 nameplate methodology to the three points the nameplate specifies exactly: locked-rotor current (bisection on |Z_r(1)| with the magnetizing branch included), locked-rotor torque (R₂ₛₜₐᵣₜ = T_LR·|Z_r(1)|²) and the rated point (stable-branch quadratic for R₂ᵣᵤₙ, intercept-corrected so R₂(s_r) lands exactly); breakdown torque is reported as a model prediction for datasheet comparison. **Mechanics**: swing equation 2H·dω/dt = T_e − T_L integrated with RK2 (dt = min(5 ms, H/20)), load torque quadratic/linear/constant with breakaway, inertia from motor+load J (kg·m²) or auto-estimated (H ≈ 0.12·P^0.15 s, warned). **Network**: Thevenin superposition V_bus = V_pre − Z_th·I — V_pre from a baseline load flow with the starting motor off, Z_th from the IEC 60909 source-path walker at c = 1.0 excluding motor infeeds, exact complex divider each step. **Starters**: DOL; star-delta (Y/3, T/3, changeover at speed threshold); autotransformer 80% tap; soft starter (voltage ramp + current limit enforced by firing-angle bisection against the Thevenin interaction); VFD reported as not-simulated (drive-controlled). Synchronous machines start on the damper cage (assumed 5% fit slip) with pull-in declared at 95% speed. **Outputs**: acceleration time, stall detection (sustained T_e ≤ T_L), peak current, min bus/motor voltage, rotor thermal ∫I²t as % of the hot-stall withstand (I_LR²·t_stall), starter changeover event, decimated trajectories (speed/current/V_bus/V_motor/T_e/T_L ≤ 400 pts) and the fitted model echo. **Frontend**: new `dynmotor.js` results modal with three SVG charts (speed+voltages vs t, current vs t, torque vs speed) — single-axis panels, crosshair + all-series tooltip, direct end-labels with leader lines, CVD-validated palette on both light/dark surfaces, data-table + fitted-model disclosures; per-motor tabs; new props (rated speed, LRT%, J motor/load, load curve+%+breakaway, changeover speed, soft-start limit/ramp/pedestal V, hot stall time, sim window) with FIELD_INFO ⓘ on both motor types in a new collapsible "Motor Starting (Dynamic)" section; `dynms:` canvas badges (accel time / stall + peak I + min V) with visibility toggle; results persisted through save/load. 20 new regression tests (nameplate-anchor reproduction, independent quadrature cross-check of the integrator within 5%, impedance-divider dip hand-calc, star-delta ⅓ current, soft-start limit, stall, sync pull-in, VFD, thermal consistency; 163 backend total) + headless Playwright end-to-end (build network → run → modal charts/tooltip/badge, dark mode, zero console errors). **Report & batch integration**: detailed calculations report gains section "6. Dynamic Motor Starting (Acceleration)" (method formulas, per-motor results, fitted model echo, ~10-row sampled trajectory table; contents page + downstream sections renumbered 7/8/9) with a `_pdf_safe` cp1252 sanitizer for engine-sourced text (the core Helvetica font rejects "≈" etc.), `dynamicMotorResults` threaded through `/api/reports/calculations` and the frontend payload; Study Manager gains the `dynamic_motor_starting` study (STUDY_DEFS + runner + status extraction incl. a `not_simulated` VFD count, checkbox in the batch modal, result distributed to `AppState.dynamicMotorResults`, per-study card counts). +3 regression tests (batch run, VFD counts, PDF section renders the "≈" warning path; 166 total) and Playwright-verified batch run + PDF generation through the real UI.~~
- ~~Plan Markup auto-circuiting (`plan-circuits.js`): ports Distribution Designer's floor-plan → final-circuit automation, the half the original port left out. Load devices carry a circuit tag (`props.circuitDbId` = a bd_db element, `props.circuitNo` = the way). `syncLoads()` counts tagged lights/sockets/FCUs per way and writes each way's `load_va`/`plan_qty` into the linked SLD `distribution_board` schedule (respecting a per-way `_manualLoadOverride`, auto-naming from the device mix), then recomputes the board's lumped load. `propagateFrom()` floods a device's tag along connected final-circuit routes; `bulkAssign()` distributes a board's connected untagged devices across ways with per-type caps (10 lights / 6 sockets); `syncRoutedLengths()` writes routed final-circuit length into each way's `cable_m`. Properties-panel Circuit editor (board + way + live VA) on load devices, Auto-assign / Sync buttons on a bd_db, and a "⚡ Sync Circuits" toolbar action (building domain). Headless Playwright verified device VA (light 100 / 2-gang socket 400), per-way load (2×100 → 200 VA) + qty, manual-override protection, tag propagation, bulk-assign caps + light/socket grouping, routed length (220 px × 0.05 = 11.0 m), and the panel/toolbar UI — no console errors.~~
- ~~Plan Markup — import legacy Distribution Designer `.json` projects (`plan-dd-import.js`): the Plan toolbar Import accepts a DD project file (detected by `proj.buildings`) and rebuilds its buildings×floors as Plan Markup floors. Transforms: origin shift (DD centres the image → offset by imgW/2, imgH/2), FP_ELS/FP_ROUTE_TYPES → `bd_*`/route-type remap (nearest symbol on a miss, original type stashed in `props._ddType`), per-floor scale carried verbatim, ids regenerated with an old→new map keeping route endpoints linked, embedded base64 rasters uploaded to `/api/plan-images` (geometry imports even if an image upload fails). DD's electrical schedule + custom symbols are out of scope. Headless Playwright verified the coordinate shift, type remaps + fallbacks with `_ddType`, route relinking, site-plan skip, non-DD rejection, and graceful image failure.~~
- ~~Plan Markup multi-floor buildings: restored Distribution Designer's floor model (lost in the port). `planMarkup` gained a `floors[]` stack — each floor a separate sheet with its own elements/routes/trenches/rooms/texts/measurements, its own background plans and its own scale calibration; the active floor is mirrored onto `planMarkup.<key>` (the live set engine/tools read unchanged) via `switchFloor()` stash/hydrate. Toolbar floor dropdown + ⚙ manager (add/rename/reorder/delete, per-floor level & height; building-domain only). Vertical riser runs: `settings.floorHeight`/`riserFactor`, `planVerticalRunM()` (floor-to-floor heights × riser factor), name-matched riser shafts (`planRiserShaft`) surfaced as routes-CSV rows + a BOQ "Riser cable — vertical" line. SLD sync / CSV schedules / BOQ aggregate across all floors (per-floor scale); DXF is a single-sheet active-floor export labelled with the floor. Legacy flat projects wrap as a single "Ground" floor; `toJSON` makes `floors[]` the source of truth. Headless Playwright verified separate-sheet isolation, per-floor scale, vertical runs, riser shafts, save/load round-trip, legacy migration, switcher UI + manager, and cross-floor board→SLD sync (no console errors).~~
- ~~The two remaining verification-gap fixes from the `testing/` discrepancy register — the two items that changed published-correct numbers (143 backend tests pass, +6 new in `test_regression.py`; regression expectations recomputed). **#8 Full IEEE 80 geometric factors** (`grounding_system.py`): new helpers `_compute_n` (Eq. 84–87 `n = n_a·n_b·n_c·n_d` — equals `max(n_x,n_y)` for square grids, correct for rectangular), `_compute_K_ii` (1.0 with rods, `1/(2n)^(2/n)` without, Eq. 90/91), and `_compute_L_M` (Eq. 88 rod-length weighting `L_c + [1.55 + 1.22·L_r/√(L_x²+L_y²)]·L_R` with rods, `L_c + L_rod` without). The square-with-rods verification case now matches the IEEE 80 mesh voltage **exactly** (was +5.7 % conservative: 792 → 749 V); rectangular / no-rods geometries are now correct too. **#9 Full IEC 61660-1 battery factors** (`dc_shortcircuit.py`): the battery now applies the standard's factors to raw nameplate inputs — EMF `E_B = 1.05·U_nB` (or an explicit `emf_v`), peak `i_p = E_B/(0.9·R_B + R_net)`, quasi-steady `I_k = 0.95·E_B/(R_B + R_net)`, and the `T_B ≈ 30 ms` battery time constant for the rise time when the branch inductance is unknown. The published CED E03-035 / IEC 61660-1 example (60-cell 120 V, R_B = 18.6 mΩ) now reproduces the **5422 A peak exactly from nameplate** (was ~11.8 % low, non-conservative). New `emf_v` field + updated `FIELD_INFO` tooltips on `dc_battery`. `testing/README.md` discrepancy register updated (#8, #9 → Resolved).~~
- ~~Four low-risk verification-gap fixes from the `testing/` cross-check discrepancy register (all opt-in / display-only — no existing project changes behaviour by default; 137 backend tests pass, +10 new in `backend/tests/test_gap_fixes.py`). **#7 Per-bus arc-flash conductor gap / equipment class** (`arcflash.py`): new `equipment_class` selector (auto / lv_switchgear 32 mm / lv_mcc_panel 25 mm / lv_cable 13 mm / mv_switchgear_5kv 104 mm / 15kv 153 mm / open_air) and a `conductor_gap_mm` override per bus, so LV switchgear (x = 1.473) can be modelled distinctly from MCC/panel; falls back to the voltage-inferred gap when "auto". **#2 Static-load motor-fraction fault contribution** (`fault.py`): `static_load` and `distribution_board` gain `motor_fraction` (0–1, default 0 = unchanged) + `motor_lrc_ratio` (default 6); the rotating share back-feeds short circuits as an induction-motor equivalent (X″ ≈ 1/LRC) per IEC 60909-0 §13 in both the source-path solve and the voltage-depression shunts. **#4 Standalone cable-sizing inputs + adiabatic basis** (`cable_sizing.py`): per-cable `standalone_current_a` / `standalone_isc_ka` / `standalone_clearing_s` allow a hand-entered check without a converged load-flow/fault solve, and an `adiabatic_basis` toggle (per-cable prop or `/api/analysis/cable-sizing` request field) switches the fault-withstand current between the default IEC 60909-0 §12 thermal-equivalent Iₜₕ = Ik″·√(m+n) and the bare-Isc adiabatic used in many hand-calcs. **#6 PV-bus generator reactive display** (`loadflow.py`): at a PV bus the generator branch/badge now reports the solver-computed reactive that holds the setpoint (net injection Im{V·conj(Y·V)} + local load Q, split by rating) instead of the scheduled Q; badge needs no change as it reads `branch.q_mvar`. All new fields carry `FIELD_INFO` ⓘ tooltips. Remaining register items (#8 full IEEE 80 geometric factors, #9 full IEC 61660-1 battery factors) deferred — they change published-correct numbers and need their `testing/` cases + regression expectations recomputed.~~
- ~~Verification of the last four engines — duty check, load diversity, DC arc flash, unbalanced load flow (`testing/case-duty-check/`, `case-load-diversity/`, `case-dc-arcflash/`, `case-unbalanced-loadflow/`). This **completes external verification of all 12 analysis engines**. **Duty check** (`duty_check.py`): peak (κ·√2·I″k = 49.38 kA), making capacity (2.5·Icu MV per IEC 62271-100 = 62.5 kA), breaking duty (Ib) and pass/fail comparisons exact over the verified fault engine. **Load diversity** (`load_diversity.py`): per-load demand factors, IEC 60439 group coincidence factor Ks (n=3 → 0.85), diversified demand (199.97 kVA), effective DF (0.783) and demand current all exact. **DC arc flash** (`dc_arcflash.py`): Stokes & Oppenlander arc operating point (I_arc 6196 A, V_arc 95.1 V) and spherical incident energy (10.82 cal/cm²) / boundary reproduce the published Ammerman/CED DC method exactly (≤0.06 %, calorie rounding). **Unbalanced load flow** (`unbalanced_loadflow.py`): collapses to the exact balanced solution when balanced; positive sequence exactly equals the verified Newton-Raphson balanced LF; the phase↔sequence (A/A⁻¹) transform and VUF = |V2|/|V1| (0.7618 %) are exact. All four verified end-to-end in the real app with genuine screenshots. `testing/README.md` scorecard now covers all 12 engines; a full IEEE 13-bus abc-frame unbalanced comparison is noted as out of scope for the simplified sequence-based engine.~~
- ~~DC engines verification (first external cross-check of `dc_loadflow.py` and `dc_shortcircuit.py`, `testing/case-dc-loadflow/` + `testing/case-dc-shortcircuit/`). **DC load flow**: reproduces an exact first-principles resistive-circuit solution to **≤0.005 %** (rectifier→cable→constant-power load: bus voltages 124.46 / 115.83 V, cable 86.34 A, loss 745 W), verified end-to-end in the app. **DC short circuit (IEC 61660-1)**: the converter current-limit is exact (charger 200 A → I_k 300 A, i_p 315 A); the battery core `E_B/R_BBr` + Laplacian network resistance + superposition reproduces the **published IEC 61660 battery peak (5422 A) exactly (0.00 %)** when given the standard's preprocessed inputs (E_B = 1.05·U_nB, 0.9·R_B). Fed raw nameplate inputs the simplified battery model reads ~5–12 % **low** (omits 0.9·R_B / 1.05·U_nB / +0.1·R_B / T_B) — logged as a non-conservative backlog enhancement above. Both verified end-to-end with genuine modal screenshots.~~
- ~~Motor-starting verification (first check of `motor_starting.py`, `testing/case-motor-starting/`): standards-anchored verification (no public example publishes a full result — toolgrit / CED course / IEEE 3002.7 give methodology only). Full-load current (153.5 A), starting current for all five starting methods (DOL 920.8, star-delta 306.9, autotransformer 589.3, soft-starter 460.4, VFD 153.5 A) and starting MVA (10.53) match hand calculations **exactly (≤0.02 %)**; the voltage dip **exactly** reproduces an independent quadratic solve of the engine's constant-PQ 2-bus model (terminal V 0.778 pu, 20.92 % dip, 0.01 %), verified end-to-end in the real app (genuine modal screenshot). Characterized the constant-PQ rotor model against the textbook constant-Z voltage divider and SC-MVA-ratio methods: they converge within ~0.2 % for moderate dips and the engine is deliberately more conservative for weak systems (large dips). Optional future enhancement: offer a constant-impedance rotor model as an alternative to the current constant-PQ (conservative) one.~~
- ~~Grounding verification (first check of `grounding_system.py`, `testing/case-grounding-ieee80/`): standards-anchored hand calculation per IEEE 80 on a square 70×70 m grid with 20 rods (ρ=400, ρ_s=2500 Ω·m, t=0.5 s, 70 kg). The app reproduces the IEEE 80 surface derating C_s (0.7429), tolerable touch/step voltages (840.5/2696.1 V), grid resistance R_g (Sverak Eq. 57, 2.7526 Ω), GPR (5252 V), K_m/K_s/K_i, step voltage (549.1 V), decrement factor D_f, and Onderdonk conductor sizing (→16 mm²) **exactly (0.000 %)**, verified both via the core sub-functions and end-to-end in the real app (genuine modal screenshot). The single divergence is the mesh (touch) voltage, **+5.7 % and conservative**, from the engine's simplified effective length L_M = L_c + L_rod (vs IEEE 80 Eq. 88 rod-weighted L_M). Documented engine simplifications (n = max(nx,ny) for square only; K_ii = 1.0 for rods only; simplified L_M) logged as a backlog enhancement above.~~
- ~~Arc-flash verification (first check of `arcflash.py`, `testing/case-arcflash-ieee1584/`): standards-anchored hand calculation per IEEE 1584-2002 (no clean public 2002 third-party worked example exists — current material is all 2018-edition, which the engine deliberately does not implement). The app reproduces the 2002 arcing-current (Eq. 1 LV, Eq. 2 MV), incident-energy (Eq. 3–5) and arc-flash-boundary results **exactly (0.000 %)** for a 480 V MCC (Iₐ 14.344 kA, E 12.82 cal/cm², AFB 1927 mm, PPE Cat 3) and a 4.16 kV switchgear case (Iₐ 19.184 kA, E 5.957 cal/cm²), verified both via the core functions and end-to-end in the real app (source tuned to 25 kA bolted, clearing time 0.20 s via a definite-time relay, genuine on-canvas badge screenshot). Confirmed engine behaviour: conductor gap auto-selected from voltage (LV = 25 mm), clearing time derived from upstream protection. One modelling limitation logged above (gap/equipment-class not user-settable).~~
- ~~Load-flow verification (first check of `loadflow.py`): reproduced a self-contained textbook Newton-Raphson example (Glover / Oregon State ESE 470 3-bus: slack at 1.0 pu, one PV, one PQ, charging-free series lines) in `testing/case-loadflow-3bus/`. ProtectionPro matches the published solution essentially exactly — V₂ = 1.0500∠−2.06° (ref 1.05∠−2.1°), V₃ = 0.9782∠−8.78° (ref 0.98∠−8.8°), slack 308.35 MW/−81.66 Mvar (ref 308/−82), gen 200 MW, load 500+j100, same 4-iteration convergence; worst error 0.4 % (slack Q). Confirmed engine constraints in the process: swing bus fixed at 1.0 pu, no line-charging shunt in the Y-bus, PV realized via a must-run generator with `voltage_setpoint_pu`. One cosmetic finding (PV-bus generator badge shows scheduled Q, not solved Q) logged above. Also surveyed and recorded (not reproducible) the ETAP official V&V PDF and the Tinghir 225/60/11 kV E3S paper — both cite external sources / omit branch impedances (`testing/reference/*-survey.md`).~~
- ~~Extended verification: second short-circuit article (220/33 kV, 10 MVA Dyn1) + LV cable sizing. **SC-2** (`testing/case-sc2-220-33kv/`): ProtectionPro reproduces the article's ETAP one-line screenshots **exactly (0.00 %)** on all 4 fault types (3φ 2.296, SLG 2.302, LL 1.988, LLG 2.309 kA) at c = 1.10 — and in doing so exposed a ~15 % error in the article's own hand-calc, which mixes a 100 MVA-base grid impedance with a 10 MVA-base transformer impedance; ETAP and ProtectionPro both convert to a common base correctly. **Cable sizing** (`testing/case-cable-sizing-lv/`, first check of `cable_sizing.py`): the IEC 60364 voltage-drop and adiabatic fault-withstand formulas reproduce the article exactly (running VD per-amp to 0.3 %; bare adiabatic 40 kA·√0.1/143 = 88.455 mm²), with the final recommendation differing only by documented, more-conservative methodology (network-integrated current/fault sourcing; IEC 60909-0 §12 thermal-equivalent Iₜₕ = Ik″·√(m+n) instead of bare Isc; conductor area inferred from R). Screenshots are genuine app renders. Site fully surveyed (`testing/reference/site-survey.md`): no further articles pair worked numbers with a ProtectionPro engine + ETAP output.~~
- ~~Configurable IEC 60909 voltage factor `c` + ETAP short-circuit verification. **Feature**: the voltage factor `c` (previously hard-coded `C_MAX = 1.10` in `fault.py`) is now a request/project parameter — `voltageFactor` on `ProjectData` (schemas.py), threaded through `run_fault_analysis` → `_collect_source_paths` / `_collect_zero_seq_impedances` / `_utility_impedance`, passed by the `/api/analysis/fault` route, and exposed in the UI (Settings → System Base → *Voltage Factor c*, persisted via `AppState.voltageFactor` in state.js toJSON/fromJSON). Default stays 1.10 (behaviour unchanged); the transformer K_T correction keeps its internal c_max = 1.10 per IEC 60909-0 §6.3.3. **Verification** (`testing/`): rebuilt the three progressive ETAP-validated worked examples from powerprojectsindia.com (no-load, +motor, +motor+lump-load) as reproducible ProtectionPro projects and cross-checked all 4 fault types (3φ/SLG/LL/LLG) × 3 cases = 12 points against the published ETAP figures at c = 1.0 — **12/12 PASS within 0.13 %** (tolerance ±2 %); impedances reproduce the article to 5 sig-figs incl. K_T and the motor sub-transient split. Screenshots are genuine renders driven headlessly through the real app UI. Qualified discrepancy: `static_load` provides no fault contribution (modeling convention, backlog item raised) — Case 3 modeled both ways (motor-equivalent matches ETAP; static under-predicts 9–29 %). 127 backend regression tests pass.~~
- ~~DC Load Flow + DC Short Circuit for DC networks (UPS / substation-auxiliary / telecom DC). **Model**: a `bus` gains a `system` select (ac default / dc) with a `voltage_dc_v` field (DC busbars render in orange with an "=" marker and are skipped by every AC engine — fault / load-flow / unbalanced / arc-flash / grounding / load-diversity / backup all filter `system=='dc'`); two new components — `dc_battery` (nominal V, Ah, internal R/L) and `dc_load` (constant-power / constant-current / constant-resistance) — plus reuse of `rectifier`/`charger` (dc_out) as DC sources and `cable` (R only) as DC branches. **DC Load Flow** (`backend/analysis/dc_loadflow.py`, POST `/api/analysis/dc-loadflow`): resistive nodal solve on bus groups (bus + closed transparent devices), Thevenin sources grounding each island (Norton current E/Rs + shunt 1/Rs to the DC reference rail), constant-power loads linearised as I=P/V and iterated to convergence, converter current-limits enforced by a re-solve loop, two-wire loop resistance 2·r·ℓ for DC volt-drop, and an operational model where an active converter holds the bus while the battery floats (≈0 A) — only a converterless island discharges the battery (avoids the non-physical charger↔battery circulating current). Reports per-bus V / %nominal / deviation / load, per-cable current / drop / loss / loading, per-source current / power / loading (with current-limited flag), islanding + voltage-band + overload warnings. **DC Short Circuit** (`backend/analysis/dc_shortcircuit.py`, POST `/api/analysis/dc-shortcircuit`): IEC 61660-1 superposition — battery source I_k=0.95·E_B/R_BBr, i_p=E_B/R_BBr, τ=L_BBr/R_BBr with R_BBr = internal + Laplacian effective resistance to the fault (pseudo-inverse of the branch conductance matrix; least-resistance-path inductance via Dijkstra); rectifier/charger treated as current-limited converters (dc_sc_factor × rated DC current, default rectifier 3.0 / charger 1.5) per IEC TR 60909-4; per-bus totals + per-source contribution breakdown; a single selected DC bus scopes the run. **Frontend**: `runDCLoadFlow`/`runDCShortCircuit` (api.js), Analysis-menu buttons + handlers (app.js), two result modals (bus-voltage / source / cable-flow table; SC total + contribution tables with the IEC 61660 method note), on-canvas draggable badges (`dclf:`/`dcsc:` keys) via new `renderDCLoadFlowBadge`/`renderDCShortCircuitBadge` (annotations.js), DC-source current lines and DC-load/battery data labels on the canvas, `dcLoadFlowResults`/`dcShortCircuitResults` state fields (persisted through save/load, reset on new/scenario-restore, with `showResultBoxes.dcLoadflow`/`dcShortCircuit` toggles), FIELD_INFO ⓘ tooltips for every new field, and dc_battery/dc_load SVG symbols. 10 standards-anchored regression tests pin the hand calcs (127 backend tests pass); headless Playwright verified DC network build → both studies → populated results, visible modals, canvas badges, zero console errors, and that AC fault / load flow on a mixed AC+DC network correctly report only the AC bus while DC SC reports only the DC bus. NOTE: capacitor and DC-motor short-circuit sources and the rigorous IEC 61660 rectifier AC-network subprocedure are not yet modelled (converters are current-limited approximations); DC cables assume a two-wire loop.~~
- ~~Reticulation / ADMD module (NRS 034-1 / CTEF100): a full LV-reticulation workspace parallel to the SLD, ported from Retic Builder Pro. **Backend** `analysis/admd.py` + `analysis/admd_data.py` implement both estimation methods — Empirical (N×ADMD×DCF with AMEU/British/None correction sets) and Herman-Beta (Beta→Normal with Cornish-Fisher skewness correction) — plus per-phase superposition aggregation, feeder subtree rollup (a feeder segment carries its kiosk + all downstream kiosks), and street-lighting/override fixed loads; exposed at `POST /api/analysis/admd` (`AdmdRequest`/`AdmdResults` schemas). 14 standards-anchored regression tests pin the formulae (76 backend tests pass). A per-phase fix makes a single-kiosk feeder total equal that kiosk's own demand (the source app undercounted it). **Frontend** new `Reticulation` workspace (toolbar switcher) with `retic.js`: kiosk cards, erf (stand) schedule tables with R/W/B phase assignment, per-kiosk & network diversified-demand badges, cumulative feeder volt-drop, transformer sizing suggestion, and a local undo stack. Editable **Load Classes** library tab in Settings (`standard-data.js` + `STANDARD_LOAD_CLASSES` in `constants.js`), mirroring the cable/transformer libraries with reset-to-defaults and localStorage persistence. Data persists in `AppState.reticulation` through save/load (`state.js`). **Integration**: reuses the existing cable library for VD; a "Push demand to SLD" bridge adds the diversified feeder as an equivalent `static_load` so fault/load-flow/duty/load-diversity studies include it. **Reporting**: `retic-report.js` exports a PDF demand + erf/cable schedule (jsPDF/autoTable). Headless Playwright verified the workspace, demand computation (Empirical/Herman-Beta), street lighting, feeder chaining, PDF download, SLD push, and the editable class library. Built on branch `feature/reticulation-admd`. NOTE: site-plan geometry (routes/trenches/drawings) and the full street-lighting circuit/pole sub-app from the source were intentionally scoped out; street lighting is a per-kiosk kVA allowance. Post-review fixes: `settings.riskZ` now actually drives the Herman-Beta risk factor (was accepted but ignored); the client-side HB service-VD current uses the Cornish-Fisher-corrected z and the correct √3·400 divisor for 3φ erven (was ~3× high); project load/new re-baselines the retic view + local undo stack (stale-view / cross-project-undo bug); retic redo now works (snapshot-after pattern, field edits undoable, deduped); SLD shortcuts are suspended while the retic workspace is active (Ctrl+Z/Y map to retic undo); "Push demand to SLD" updates the existing Reticulation Feeder load instead of duplicating; `AdmdFeederTotal.streetLightKVA` no longer stripped from the response; short custom class libraries can't 500 (`classes[2]` fallback guarded). Documented deviations from the source app: empirical feeder rollup honours per-kiosk `admdOverride` (source used only the project ADMD); frontend VD uses R·cosφ+X·sinφ with no snaking/additional allowances; UCF remains display-only (as in the source); the 3φ-class + "3 Phase"-erf 9× quirk is inherited from the source (use phase colours for 3φ-class erven). +2 regression tests (78 total). The settings bar gained a "Risk z (Herman-Beta)" field with an explainer tooltip and a live "≈ N% risk of exceedance (M% confidence)" hint (standard-normal exceedance via Abramowitz–Stegun), disabled under the Empirical method. **Quick Build** panel (⚡, top of the kiosks pane): one click appends N kiosks × M erven pre-filled with a chosen service cable + worst-case service length and feeder cable/length, daisy-chained (continuing from the last existing kiosk) or star-fed from the source — one undo step, kiosks added collapsed. The same quick defaults seed "+ Add Kiosk"/"+ Erf", and the per-kiosk batch button follows the "Erven / Kiosk" setting ("+ 6 Erven" by default). Quick defaults persist in `reticulation.settings` (older projects backfill). Retic cable dropdowns now group LV (≤1 kV) cables first, MV below. Headless Playwright verified: build topology/cable/length fill, phase round-robin, demand + service/feeder VD computed, batch button count, single-step undo. **Minisubs** are first-class sources (`reticulation.minisubs`, first id `source` so legacy `fedFrom` resolves): ADMD diversity is applied per minisub across all kiosks fed transitively from it, per NRS 034 practice, and the network total is the SUM of per-minisub diversified demands × an editable network diversity factor (`settings.networkDiversity`, default 1.0) — previously the app lumped all kiosks into one diversified total, understating multi-minisub designs (2×4-kiosk example: 242.4 vs 218.16 kVA). Summary panel shows a block per minisub (editable name, demand, per-minisub transformer suggestion, delete re-points kiosks), Fed From dropdowns group Minisubs/Kiosks, Quick Build gained a Feed From minisub picker (chain continues within that minisub's group), deleting a kiosk re-points its children to its own parent, Push-to-SLD creates/updates one `Retic: <minisub>` static load per minisub, and the PDF report gained a per-minisub demand/TX table + Minisub column on the kiosk schedule. +2 regression tests (80 total: per-minisub grouping incl. chained kiosks, NDF scaling, legacy single-source parity); headless Playwright verified grouping, rename, NDF, per-minisub SLD push, and minisub deletion re-pointing.~~
- ~~DB circuit default loads & quick-add: the distribution-board circuit-schedule editor (`dbschedule.js`) gained a `DB_LOAD_TYPES` library (`constants.js`) of common load types (Lighting, Socket Outlet, Geyser, Stove, Air Conditioner, EV Charger, Heat Pump, 3φ Motor, Spare) each carrying default VA/unit, demand factor, poles, breaker, curve and cable size. An "Add load" dropdown + unit-count field adds a way pre-filled from the selected type; quick-add chips add "+ 10 Lights" and "+ 5 Sockets" (single count-based circuits) and "+ 4 Light circuits" / "+ 6 Socket circuits" (batches). The Description cell is also a `datalist` combo that autofills a preset's defaults when picked into an empty way. All added ways round-robin R/W/B for balance and flow into the existing lumped-load recompute. Headless Playwright verified circuit counts, wattage/DF/breaker/cable fills, 3φ phase handling, description autofill, and the rendered controls.~~
- ~~UX & accessibility audit — remaining open findings fixed (C2, C3, C4, H14, H15, H16, H21, H23) plus PROT-21: new shared `frontend/js/ui.js` module provides `UI.toast` (used on desktop; mobile `showToast` delegates to it — H16), promise-based `UI.alert/confirm/prompt` modal dialogs replacing all ~41 native dialog sites across project/app/dbschedule/standard-data/reports/tcc/templates/properties (C2), a ref-counted `UI.setBusy` full-screen busy overlay wired into `_setBusy` so the canvas locks while any analysis runs (H15), and `ModalFocus` which traps+restores focus on every `.modal` (C3). `#sld-canvas` gains `tabindex`/`role`/`aria-label` and arrow-key spatial navigation between components when keyboard-focused (C3); Select-mode port drags now show the wiring cursor + status hint (C4); `--text-muted` darkened to `#6d6d6d` for AA (H14); non-General property sections default to expanded (H21); dark-mode `.af-*` result-table rows and warning items get readable overrides (H23). PROT-21: the generic gG fuse pre-arcing characteristic (frontend `FUSE_CURVES_GG` + backend `_FUSE_CURVES_GG`, kept byte-identical) was re-anchored at the fast end so pre-arcing time reaches 0.1 s at 8×In, satisfying the IEC 60269-1 0.1 s gate (previously ~0.17 s at the gate); the affected arc-flash clearing regression assertion was updated to the new curve value. 62 backend tests pass; headless Playwright verified module load (no console errors), toast/dialog/busy behaviour, canvas a11y attributes, and expanded-by-default sections. NOTE: the gG family remains a single representative shape, not the per-rating min/max corridor of IEC 60269-1 Table 4 (licensed) — precise grading still needs manufacturer data.~~
- ~~Per-device TCC grading button: Button in CB/fuse/relay properties panel to open focused TCC window with upstream protection devices for grading~~
- ~~Persist result box positions: analysis result boxes retain their dragged positions across simulation re-runs and project save/load; analysis results are saved with the project so boxes appear on load; "Reset Result Box Positions" button in View menu to restore defaults~~
- ~~Properties panel sections: organized component properties into collapsible sections grouped by analysis type (General, Fault Analysis, Load Flow, Arc Flash, Grounding, Cable Sizing, Protection Settings)~~
- ~~Mobile interface: responsive layout with bottom navigation, slide-up component/analysis/properties/menu sheets, pinch-to-zoom and pan touch gestures, floating action buttons for mode and zoom, tap-to-place components~~
- ~~Overlay fault current markers on TCC~~
- ~~TCC comparison mode (side-by-side before/after)~~
- ~~Toolbar reorganization (two rows)~~
- ~~TCC CB instantaneous line dragging (magnetic pickup)~~
- ~~TCC editable settings panel for selected curves~~
- ~~Customizable report templates~~
- ~~Scenario comparison reports~~
- ~~Settings schedule export (CSV)~~
- ~~Pre-built network template library (substation, industrial, residential, generator)~~
- ~~Component grouping (Ctrl+G / Ctrl+Shift+G)~~
- ~~Multi-page diagrams (sheet tabs, add/delete/rename)~~
- ~~Print / page layout (PDF with title block, legend, border)~~
- ~~Wire diagonal/spline routing~~
- ~~Off-page connectors (cross-page electrical links)~~
- ~~Draggable bus resizing with dynamic connection ports~~
- ~~Distributed generation: Solar PV and wind turbine models~~
- ~~IEEE 1584-2018 arc flash calculations with incident energy, PPE categories, and arc flash boundary~~
- ~~Per-bus working distance and electrode configuration settings~~
- ~~Arc flash on-diagram annotations with PPE-category color coding~~
- ~~Arc flash improvement recommendations per bus~~
- ~~Help modal with comprehensive documentation~~
- ~~Fix bus detection: validation and unconnected-port warnings now use dynamic bus ports instead of static placeholders~~
- ~~Voltage depression during fault (Zbus matrix, retained voltage at all buses)~~
- ~~Time-varying voltage profile (sub-transient/transient/steady-state)~~
- ~~Motor reacceleration voltage recovery post-fault clearing~~
- ~~Color-coded voltage sag map on SLD~~
- ~~Distance relay (21) TCC curves with mho characteristic R-X inset~~
- ~~Auto-coordination engine: topology-aware relay/CB grading~~
- ~~Protection relay miscoordination detection~~
- ~~Arc flash label PDF export (server-side)~~
- ~~Relay-CT-CB linking (associated CT and trip CB)~~
- ~~Server-side PDF report generation (fpdf2)~~
- ~~Parallel cable impedance modeling~~
- ~~CT saturation modeling: CT ratio and burden effects on relay operating times~~
- ~~Directional relay curves: Directional overcurrent (67) curve plotting~~
- ~~Detailed calculation formulas and standard references in HELP documentation for all analysis functions (Fault, Load Flow, Arc Flash, Cable Sizing, Duty Check, Grounding, Load Diversity, Motor Starting, TCC)~~
- ~~Drag devices between tabs: Move devices to custom tabs via drag-and-drop~~
- ~~User-defined curve data: Import custom manufacturer TCC data points (CSV)~~
- ~~UPS / Rectifier / Battery Charger: DC system source components for battery-backed systems~~
- ~~Directional flow arrows on wire connections (animated direction indicators)~~
- ~~Unbalanced load flow: Three-phase asymmetric network analysis using symmetrical component method~~
- ~~NEC ampacity tables: NEC Article 310.16 ampacity data alongside existing IEC 60364~~
- ~~Sequence impedance editor: Positive, negative, zero sequence impedance fields for utility, generator, cable, and motor components~~
- ~~DC arc flash analysis: Stokes & Oppenlander method with DGUV-I 203-077, DC arcing current, incident energy, PPE categories, and arc flash boundary~~
- ~~SANS 10142 automatic compliance checks: LV voltage tolerance (±10%, NRS 048-2), cable protection coordination (In ≤ Iz, Cl. 5.5.2), minimum conductor size (Cl. 5.6.3), transformer neutral earthing (Cl. 8.3.1), maximum demand vs supply capacity (Appendix B), earth fault disconnection current (Cl. 5.5.6)~~
- ~~Source/generator load split: Fix incorrect proportional split when utility source connects via transformer — generators now report at rated output, utility takes residual~~
- ~~Detailed calculations report: Multi-section PDF with formulas and intermediate values for all analysis types (fault, load flow, arc flash, cable sizing, motor starting, duty check, load diversity, grounding)~~
- ~~Help documentation update: Comprehensive help with all 22 component types, new Studies tab (cable sizing, motor starting, duty check, load diversity, grounding), DC arc flash docs, unbalanced load flow, TCC enhancements, compliance details, updated shortcuts, templates, export options, scenarios, and mobile support~~
- ~~Apparent power (S) display in load flow annotation badges~~
- ~~Toolbar quick access bar: Favourited/commonly used tools pinned to the toolbar for one-click access, with editor modal and localStorage persistence~~
- ~~File manager: folder-based project organization, project/folder rename, move projects between folders~~
- ~~Hide busbar connection points once connected: bus ports are hidden after a wire is attached, reducing visual clutter~~
- ~~Independent flow arrows: Load flow arrows show independently when load flow is run; fault current arrows show only when a specific bus is faulted, with direction toward the faulted bus~~
- ~~Fix sheet display bug: annotations, data labels, overload flags, and unconnected port warnings from other sheets no longer bleed through to the active sheet~~
- ~~Fix fault branch current display: upstream branch fault currents now account for transformer turns ratios by converting per-unit current to actual kA at each element's operating voltage level~~
- ~~Fix cable sizing warnings: warnings no longer recommend the same cable already selected; warning badges and results table now show tooltip explanations for why the cable is near limits~~
- ~~Reports menu: moved report exports (CSV, PDF, templates, settings schedule, arc flash labels, calculations, scenario comparison) from File menu to dedicated Reports menu~~
- ~~Voltage-filtered searchable cable dropdown: cable type selector now filters by connected bus voltage (LV/MV) and includes a searchable text input for quick cable lookup~~
- ~~Fix print/page layout PDF export: add dynamic jsPDF loading with fallback when CDN script fails to load~~
- ~~Fix single-line diagram missing from PDF reports: rasterize SVG client-side and embed in server-generated PDF~~
- ~~Connection warning logic: unconnected port warnings on buses only shown when the bus has zero connections, not on every empty port~~
- ~~Auto voltage propagation: components connecting to a bus automatically adopt its voltage level; bidirectional-smart logic propagates source voltages to buses at defaults; transformers bridge voltage zones with HV/LV winding matching; bus voltage changes prompt confirmation before updating all connected equipment~~
- ~~Mini SLD panel in TCC: vertical protection-path diagram alongside TCC chart with simplified IEC symbols, component names, ratings, clickable device selection, and collapsible panel~~
- ~~Show active project name in toolbar: display the current project name next to the app title, clickable to rename~~
- ~~Fix mini-SLD collapse bug: collapsed panel now shows narrow 28px strip with reopen button instead of disappearing entirely~~
- ~~Mini-SLD grading endpoint selection: set a downstream protection device as path endpoint from device list to show only source-to-endpoint path on mini-SLD~~
- ~~Delete custom TCC tabs: custom tabs now show × close button to remove them, reassigning devices back to their voltage tabs~~
- ~~Auto-save toggle: toolbar button to enable/disable periodic auto-save (every 2 minutes), saves to database for existing projects and localStorage backup for unsaved projects, with preference persistence and page-unload backup~~
- ~~Project details tab: project metadata (project number, client/company, engineer, checked by, approved by, revision, date, description, company logo) shown in properties panel when nothing selected; all details appear on PDF report cover pages~~
- ~~Project revision timeline: horizontal timeline strip above the status bar with clickable revision dots (up to 20 revisions), preview panel with diff stats and confirm-before-restore, revisions created on every manual and auto-save, stored in backend DB for saved projects and localStorage for unsaved projects~~
- ~~Generator/PV/Wind data labels: display static property labels (rating, voltage, power factor) next to generator, solar PV, and wind turbine components on the canvas, matching transformer label behavior~~
- ~~Bus names in result boxes: annotation badges (Fault, Load Flow, Arc Flash, Unbalanced LF) now display the bus name in their label for easy identification on busy diagrams; hover highlighting links annotation boxes to their bus component with a blue glow effect~~
- ~~Fix array reduction error: Fix load flow crash when all buses are swing type (e.g., single bus with utility source) causing empty mismatch array in Newton-Raphson solver~~
- ~~Cable impedance reset to defaults: when a standard cable type is selected, impedance fields modified from library defaults are highlighted with an orange indicator and a reset button to restore the standard value~~
- ~~Cable library table completeness: settings modal cable library now shows all attributes including insulation type, zero-sequence R₀ and X₀ columns~~
- ~~Single/two/three-phase load connectivity: static_load components now support 1P-A/B/C, 2P-AB/BC/CA, and 3P phase_connection property with proper symmetrical-component sequence injection in unbalanced load flow; phase % fields hidden for non-3P connections; calc modal shows unbalanced LF results~~
- ~~UX audit fixes (H17-H23, M31-M42, low items): undo now covers pages/groups/active sheet and page deletion + paste are undoable; cable/transformer/CB/fuse library edits persist to localStorage with confirm-guarded reset-to-defaults; R-key rotation and Space+drag panning implemented; dark-mode result badges and data labels readable; failed DB saves stay dirty with persistent error message and local backup (declining backup restore no longer deletes it); drags no longer stick when the mouse leaves the canvas; Escape closes modals, modals carry dialog ARIA roles, visible :focus-visible outlines; client/company project fields survive save/load round-trip; result badges auto-stack per bus instead of overlapping; single-bus fault scope is an explicit confirm; analysis buttons disable while requests are in flight with a 60 s API timeout; component names escaped in SVG labels/badges; multiplicative zoom with Ctrl+=/Ctrl+- keyboard zoom; arrow-key nudge (Shift = 5 grid units); annotation drags set the dirty flag; minimap collapse toggle and active-sheet filtering; "Undo available (N of M)" status; numbered unique paste names; emoji-free duty badge; popup-blocker detection for print preview; empty-canvas hint; help text corrected~~
- ~~Component-data audit fixes (H12, H13, H16, M25-M30, M2/M4/M9 frontend, low items): cable library R values corrected to 90°C AC resistance (Cu ×1.275, Al ×1.282, PVC ×1.20 at 70°C); 20 MVA 33/11 kV library unit changed YNd11→Dyn11 with NECRT note on 132/33 kV units; dead utility X₀/R₀ knob and unused cable R₂/X₂ fields removed; min/max/step constraints rendered and clamped with invalid-input highlighting; property edits debounced to one undo step per field edit with a status notice when results are cleared; library cable selection now copies R₀/X₀ and transformer selection syncs grounding from vector group; cable voltage-drop display fixed to √3·I·R/V; motor kVA display uses kW/(η·pf) and transformer K_T uses LV-side c_max to match the engines; prop values HTML-escaped in the properties panel; loads/sources behind a terminal cable/transformer now warn instead of silently dropping from analysis; missing Swing bus downgraded to a warning; 1% tolerance on cable bus-voltage check; validation for non-positive cable R+X, generator Xd'', utility X/R; path cap in upstream protection trace; MV breaker/fuse library entries relabeled (VCB / IEC 60282-1 back-up); misc default fixes (motor X/R 2.4, CT Rct 0.3 Ω, wind kVA unit, rectifier pulse select, capacitor steps ≥ 1)~~
- ~~Critical audit fixes (C1-C8): arc flash Cf voltage factor un-reversed (LV incident energy was understated 33%); spurious ×3 removed from utility/generator/solar/wind zero-sequence source impedance (SLG currents were ~40% low); IEEE 80 grounding conductor sizing unit error fixed (~×506); motor starting now injects the full locked-rotor kVA (dips were ~3× low); compliance engine connectivity walker fixed to real wire field names (most standards cross-checks were dead code); TCC coordination/miscoordination/auto-grading now refer currents across transformer voltage levels; undo history cleared on every project load path (Ctrl+Z could overwrite one project with another); dirty-guards and beforeunload warning protect unsaved work; Ctrl+V paste un-shadowed; marquee/Ctrl+A select only the visible sheet~~
- ~~Backend engine audit fixes: arc flash relabeled honestly as IEEE 1584-2002 with corrected MV/LV distance exponents and upstream-only slowest-device clearing time; parallel/ring source paths no longer dropped in fault tracing; Gauss-Seidel PV-bus Q sign fixed; sequence injections ×3 and Dyn transformer Y0 shunts fixed in unbalanced load flow (per-component Z0 solve); swing bus auto-promoted behind transformers; motor MVA = kW/(η·pf) per IEC 60909-0 §3.8 everywhere; K_T from LV-side c_max; LV c-factor 1.10 per IEC 60909 Table 1; branch contributions scale to bus totals for unbalanced faults; cable sizing temperature-corrects its internal 20°C table, fails on ambient ≥ conductor temp, uses real branch pf, and reports unknown instead of pass without load flow; duty check verifies making capacity (IEC 60947-2 stepped n / 2.5× MV) with a <10% warning band; load diversity no longer double-counts HV-side loads; route layer exposes cable sizing parameters~~
- ~~TCC/compliance audit fixes: instantaneous (50) element and Definite Time curve support; device voltage resolved per transformer winding side; sequence-of-operation restricted to source-side devices; phase vs earth-fault elements graded separately at ik3/ik1; pairwise coordination uses real path topology; ACB drawing matches trip math; CB trip-class constants recalibrated (t(6×Ir)=class); fuse curves labeled pre-arcing with 1.2× total-clearing for downstream margin; transformer damage curve clamped to ≥3.5×Ir with 12×In inrush marker; CTI per device-pair type (relay/fuse/I²t); compliance voltage limits ±10% LV / ±5% MV; breaking duty vs Ib and peak vs making capacity; SANS cable checks resolve voltage via topology~~
- ~~Standards-anchored regression test suite (backend/tests/test_regression.py): IEC 60909 Ik3/Ik1/κ hand calculations, IEEE 1584-2002 Cf and normalized energy, IEEE 80 Eq. 37 conductor sizing, motor-start dip band, balanced-through-unbalanced solver equivalence, NR convergence — run via pytest in the backend Docker image~~
- ~~Code-review fixes (post-audit, 10 findings + below-cut items): fault.py and components.js source/upstream path DFS now bound node expansions (not just completed-path count) so meshed/source-isolated networks can't hang; dc_arcflash.py passes the DC arcing current into get_clearing_time (was defaulting the instantaneous branch off → ~10× energy inflation); arc-flash clearing-time BFS skips open CBs/ties and the Cf/distance/arcing-current voltage split uses ≤ 1 kV per IEEE 1584-2002; pdf_reports.py calculations cover reads renamed client/company keys; load_diversity skips unset-voltage buses in LV-side classification; balanced/unbalanced load flow share one find_swing_candidate helper; unbalanced Y0 shunts treat zigzag windings as delta-equivalent (matches fault.py); frontend Calculation Details c-factor/K_T use 1.10 to match the engine (vacuous backend ternaries cleaned up); select rendering compares via String() so numeric-coerced props re-select correctly; cable-sizing "unknown" status renders gray (badge + table + tooltip) instead of green/contradictory; all frontend "IEEE 1584-2018" references corrected to 1584-2002 incl. printed arc-flash labels; canvas idle mousemove skips screenToWorld (no forced layout per pointer move); compliance making-capacity mirrors duty_check (2.5× MV / IEC 60947-2 stepped n LV) and uses a cached wire-adjacency index; TCC coordination backfills earth/phase test points after a single-fault-type study, shares one trip-time helper across the three grading engines, adds the <10 s relevance gate to miscoordination detection, and skips fuse-fuse pairs in auto-coordination; project JSON carries dataVersion 2 and migrates legacy 20 °C cable resistances to operating temperature on load; component-library localStorage payload is versioned and warns when shipped defaults have advanced~~
- ~~Remaining audit Low items: motor starting now analyses synchronous motors (asynchronous amortisseur start, kVA rating convention) alongside induction motors, and models the starting method (DOL / star-delta / autotransformer 80% / soft-starter / VFD) as a locked-rotor current reduction (I ∝ V², VFD ≈ full-load current); results report machine type and starting method. Arc flash boundary is now evaluated for both the full and reduced arcing current and reports the worst (largest) distance, matching the worst-of incident-energy treatment. Added regression tests for synchronous-motor analysis and reduced-voltage-starter dip reduction (21 tests pass). Note: locked rotor remains a conservative constant-PQ model (constant-Z would require load-flow shunt support); the cable-sizing "no library match" note, the load-flow series-chain P/Q display duplication, and the prompt()/confirm() → modal UX refactor are left as documented, non-result-affecting limitations.~~
- ~~Cable library voltage filter fix: selecting a library transformer now propagates its winding voltages to connected zones (previously only manual edits did); wiring into a pre-configured transformer applies the facing winding voltage to an all-defaults zone; the cable-type dropdown filter also reads directly connected transformer windings, so a cable on a 420 V secondary lists LV cables instead of the stale 11 kV set~~
- ~~Static load calculated current: properties panel shows a "Calculated Values" section with rated current derived from the entered kVA rating (phase-connection-aware: √3·V_LL for 3P, V_LL for 2P, V_LN for 1P, matching the unbalanced load flow) plus a demand current row when demand factor ≠ 1; the section refreshes on field commit (also fixing stale phase-% field visibility when phase connection changes) and the View Calculations modal uses the same phase-aware formula~~
- ~~Load & generation control (islanding + merit-order dispatch): load flow (balanced + unbalanced) is now island-aware — each electrical island gets its own slack (utility connection bus, else user Swing bus, else the lowest-merit source), so opening the utility CB and running on generators/solar no longer fails; sourceless islands are reported de-energized (0 V, energized flag) instead of making the solve singular; sources carry dispatch_priority (solar/wind 1, generator 2, utility 3), dispatch_mode (must_run/merit_order) and utility allow_export props; merit-order dispatcher allocates output up to island demand with curtailment (and warnings) when generation exceeds demand and no export path exists; results carry a per-source dispatch table (available/dispatched/curtailed MW, balancer role) shown in a post-run Generation Dispatch modal; validation counts solar/wind as sources and downgrades "no path to source" to a de-energized warning; voltage-error badges no longer render for informational warnings; 7 new regression tests~~
- ~~Standby dispatch mode + utility supply capacity: generators default to dispatch_mode=standby — idle while a utility balancer has capacity, dispatched (by priority) only for demand beyond the utility supply_capacity_mva (new utility prop, 0 = unlimited) or when islanded (standby joins the merit order); post-solve warning when the utility exceeds its declared capacity; curtailed role/MW now means actively-cut output (merit partial dispatch no longer mislabelled); swing-bus source badges attribute merit-dispatched output per source instead of a rating-proportional droop split, so irradiance/wind % visibly scale PV/WTG results; solar/wind canvas labels show availability-scaled output (e.g. "194 kW @ 50% → 97 kW"); 2 new regression tests (30 total)~~
- ~~Inverter/turbine count clarity: solar rated power relabelled "Rated Power (per inverter)" and count "No. of Inverters (× rated power)" (same for wind turbines); properties panel Calculated Values now shows Total Plant Rating (e.g. "3 × 100 kW = 300 kW") and Available Output at reduced irradiance/wind %, refreshing on edit; canvas data label shows the multiplied total ("3×100 kW = 300 kW") instead of the per-unit rating with a separate count line~~
- ~~Swing-bus source attribution fix: loads and dispatched source injections at the swing bus are invisible to NR slack accounting, so the utility branch annotation showed phantom import/export when PV served a local load (e.g. curtailed PV covering the whole load still showed the utility carrying ~the load current); utility badges and the swing-bus source pool now add the local load (P and new Q tracking, capacitors counted as Q supply) and subtract local dispatched generation, so a PV covering the load leaves the utility at ~0 kW (losses + reactive only); generator-island badges likewise now show the load they serve; regression test pins both layouts (31 tests)~~
- ~~Load flow bus boxes always show P/Q/S/I: the green bus annotations previously hid power/current lines whenever the bus NET injection was ~0 (pass-through buses, swing buses serving local load); backend now reports per-bus through-power (outgoing branch flows + local load, new p_through_mw/q_through_mvar fields, zero for de-energized buses which display a DE-ENERGIZED line) and the badge always renders P, Q, S and I from it, falling back to net injection for results saved before the field existed; regression test pins a radial feeder where all buses report ≈ the load (32 tests)~~
- ~~Loss-compensating curtailment + standby badge fix: with a no-export utility, curtailed sources are now re-dispatched to also cover measured network losses (up to 3 dispatch/solve passes with overshoot back-off), so PV covering the load leaves the utility at ~0 kW instead of importing the losses (Bouchard Findlayson: utility import 12.1 kW → 0.0 kW, PV 159.8 → 171.4 kW with remaining headroom curtailed); idle standby sources are recorded with explicit zero output so their canvas badges no longer show full rated power~~
- ~~Free-position bus attachments: wires connect anywhere along a bus bar (grid-snapped "at_<x>" ports) instead of fixed 40px slots — clicking a bar now selects the bus (the port hit-circles that blanketed it are gone, replaced by a fat hit-line + hover tooltip), attachment dots render exactly on the bar at each connection, the target bus glows with a snap dot during wire drags, and resizing keeps every connection at its canvas position (previously wires jumped or collapsed to the bus centre); legacy top_i/bottom_i port ids migrate on load with a render-time fallback~~
- ~~Project save/load data-loss fix: backend ProjectData/Component/Wire schemas now preserve unknown fields (extra=allow) — previously every save silently dropped dataVersion (so the one-time cable-resistance migration re-ran on EVERY load, compounding cable R by ~1.28x per save/load cycle), plus pages, groups, wire bend points and route mode; repaired the compounded cable resistances in saved projects (Bouchard Findlayson had cables at 1.275^6-8 x library values, causing overstated volt-drops and a diverging load flow) and stamped dataVersion~~
- ~~Right-click context menu on the SLD canvas: context-sensitive menu (new contextmenu.js) replacing the suppressed browser menu — CB/switch get a one-click Open/Close toggle (same commit ritual as a panel edit: results cleared, undo snapshot), buses get "Run Fault Analysis Here" (reuses the validated single-bus flow via window.AppActions), protection devices get View TCC Grading; all components: Properties, Rotate, Copy/Cut/Duplicate/Delete, Group/Ungroup with shortcut hints; wires: Add Bend Point at cursor, per-wire route mode (orthogonal/diagonal/spline — first UI for wire.routeMode), Delete; empty canvas: Paste Here (at cursor), Select All, Zoom to Fit; right-click selects the target unless already in a multi-selection; dismisses on outside click/Escape/action, clamps to viewport, inherits dropdown + dark-mode styling~~
- ~~CB/fuse ratings visible by default: showDeviceLabels now defaults to true (was false), so circuit breaker labels (type, trip rating, Icu, Ir/Im pickups) and fuse labels (type, rating, Icu) render on the canvas without hunting for the View → Device Labels toggle; the toggle still hides them and its menu check-state matches the new default~~
- ~~Generator Full Load Current in properties panel: the Calculated Values section (retitled from Per-Unit Values, base folded into the X"d row label) now shows FLA = S/(√3·V) from the machine rating, refreshing live when rating/voltage/Xd" commit~~
- ~~Solar/wind Full Load Current in properties panel: Calculated Values now shows FLA for solar PV (S = total kW ÷ inverter efficiency per the engine convention, so I = S/(√3·V)) and wind turbines (total MVA/(√3·V)); rating-based and independent of irradiance/wind % (Available Output tracks those), refreshing live on rating/count/voltage/efficiency edits~~
- ~~CB trip currents shown in brackets: the Thermal Pickup (Ir) and Magnetic Pickup (Im) field labels in the properties panel now append the resulting trip current — Ir = In × thermal, Im = Ir × magnetic per the TCC convention (e.g. "Thermal Pickup (630 A)", "Magnetic Pickup (6.30 kA)") — updating live as trip rating or either pickup commits; canvas device labels split Ir/Im onto their own lines with the same bracketed amps~~
- ~~Generation Dispatch reopenable: new Analysis → Generation Dispatch menu item re-shows the dispatch summary modal from the last load flow results (status-bar hint when no results yet); the auto-popup after noteworthy runs is unchanged~~
- ~~Collapsible side panels + component ribbon: both the Components sidebar and Properties panel gain collapse buttons (thin edge tabs re-expand); View → Component Ribbon swaps the sidebar palette for a compact icon strip below the toolbar (grouped by category, tooltips for names, same drag-drop via shared .palette-item handlers); layout choices persist in localStorage (protectionpro-layout); properties collapse button placed as a header sibling so Properties.show() title rewrites cannot destroy it~~
- ~~Distribution Board component (Loads): build a DB via a circuit-schedule editor (new dbschedule.js modal — per-way description, poles 1P/3P, phase R/W/B, breaker rating + curve, earth-leakage group, cable size/length, load VA, demand factor; add/remove ways with live totals: connected kVA, diversified demand + amps, phase balance). The board is analysed as a lumped load: on save the editor derives static-load-equivalent props (rated_kva, aggregate demand_factor incl. board diversity, phase_a/b/c_pct with R→A/W→B/B→C and 3P ways split /3) which loadflow/unbalanced/load-diversity read via a static_load type alias; DB symbol + canvas label (ways + demand kVA), Calculated Values panel section, Edit Circuit Schedule button + context-menu item, voltage propagation, Escape commits edits; PDF reports gain a per-board Distribution Board Schedule section (db_schedules, in the Full Report template + report modal checkbox); regression test pins DB demand through both solvers (33 tests)~~
- ~~Dark-mode colour audit: buses were invisible (stroke #222 on the #1a1a2a canvas) along with transformers/switches (#333), cables/CTs/PTs/loads (#555), and low-contrast utility/relay (dark blue) and motor (dark purple) symbols; added a hue-preserving dark palette block in symbols.css (bus #e0e0ee, neutral greys lifted to #a8a8c0-#c0c0d0, utility/relay #64a5f5, motor #ba68c8, generator/arrester #66bb6a, CB #ef5350, capacitor #4dd0e1), DC enclosure rects (UPS/rectifier/charger) gained outlines in both themes, unconnected-port warning circles brightened; verified with a computed-contrast audit across every SVG element class (all remaining <3:1 entries are intentionally transparent hit areas)~~
- ~~DB schedule Excel import/export + bigger entry boxes: vendored SheetJS (js/lib/xlsx.full.min.js, same pattern as jsPDF); Export Excel writes the schedule as a formatted .xlsx named after the board; Import Excel accepts .xlsx/.xls/.csv with fuzzy header matching (way/desc/poles/phase/breaker/curve/EL/cable/length/load/DF), value validation and a replace-confirm when ways exist; round-trip verified. Editor modal widened to 1280px (specificity fix over the 720px base modal cap) with 32px-tall inputs and a full-width description column; totals now computed exactly instead of via the rounded aggregate demand factor~~
- ~~DB schedule phase-balance bars + Auto Balance: the circuit schedule editor shows three load-balancing bars at the top (SA phase colours R red / W white / B blue, widths relative to the heaviest phase, per-phase diversified kVA and % labels, animate on edit); new Auto Balance button reassigns single-phase ways across R/W/B with a largest-first greedy (LPT) that leaves 3P ways untouched and reports the result in the status bar (verified optimal on discrete sets and exact 33/33/33 on balanceable ones)~~
- ~~Generator minimum load (wet-stacking floor) + solar curtailment link: generators gain min_load_pct (default 30% with sourced info tooltip — manufacturers recommend 30-35%, NFPA 110 exercises at >=30% nameplate; settable 0-100, 0 disables); the dispatcher raises any running generator to its floor, protects the floor from the no-export curtailment pass, and when an islanded generator balances the island it curtails solar/wind so the gen carries at least its minimum (warning if demand is too low even with zero renewables); 3 new regression tests (36 total)~~
- ~~DB schedule spreadsheet-style entry: cell edits no longer re-render the table (bars/totals refresh in place so focus survives), inputs select-all on focus, Enter/ArrowDown move down a column (Enter on the last row appends a way), ArrowUp moves up, and multi-row TSV paste from Excel/Sheets fills cells starting at the focused cell with value normalisation (poles/phase/curve) and auto-appended rows~~
- ~~Sequential generator control (load-demand start): generators gain gen_control (droop = rating-proportional sharing, the default/historical behaviour; sequential = DSE/ComAp-style paralleling — lead set fully loaded before the next starts) and start_threshold_pct (default 90%, shown only for sequential; next set starts when running capacity utilisation exceeds it; start sequence = dispatch_priority); islanded sequential sets commit in sequence, committed non-last sets run fixed at their fill-first allocation (respecting the last set's min-load floor so a freshly started set never backfeeds — the lead backs off instead), the last committed set balances the island, uncommitted sets show role "Off (sequence)" with 0 kW badges; warnings for sets brought online / held off; 3 regression tests (39 total)~~
- ~~Generator-balancer attribution fix (found verifying on Bouchard Findlayson): a generator balancing an island showed -0 kW in the dispatch table (per-swing-bus slack formula read the wrong bus when a user-labelled Swing overrode the balancer bus) while its canvas badge showed FULL rated output (fallback when absent from the dispatched map); generator balancers are now attributed island-wide (slack = island net injections + load - dispatched) and the badge is synced to the same figure (BF: Gen200KVA -0/170 kW -> consistent 60.1 kW @ 35% loading)~~
- ~~Scenarios never change device names + configurable scope: loading a scenario preserves every current component name (matched by id — renames made after saving a scenario survive loading it; components existing only in the snapshot keep their saved names); the save form gains "Applies" tick boxes (Switching states / Electrical settings / Diagram layout, all ticked by default and stored on the scenario) — all ticked loads the full snapshot as before (incl. topology), any unticked switches to overlay mode applying only the chosen categories to matching components without touching topology; scenario list and load-confirm show what each scenario applies; legacy scenarios without flags behave as full snapshots~~
- ~~Cable library IEC audit: verified all 78 entries computationally — R at operating temperature matches IEC 60228 Table 2 (class 2 stranded) x temperature factor to <0.05% for sizes <=120mm²; ampacities align with SANS 10142-1 Table 6.8 buried-direct ratings (LV) and manufacturer 11/22/33 kV buried tables; X and R0/X0 (3.4-4.2x / 2.8-3.2x) within 3-core armoured literature ranges; monotonicity clean. FIXED: AC resistance for large conductors omitted IEC 60287-1-1 skin/proximity effect — corrected 29 values (Cu 150-400mm² and Al 185-400mm² across XLPE 0.4-33kV and PVC families, e.g. 400mm² Cu 0.0599->0.0649 Ω/km ≈ manufacturers' 0.065; Cu 150 0.1581->0.1601), R0 scaled to preserve each family's design ratio; existing projects keep their stored cable props (library reset buttons and new selections pick up corrected figures)~~
- ~~Export Diagram (PDF): new File-menu item next to SVG/PNG — client-side via the vendored jsPDF, reusing the existing export rasterizer (2x, white background); auto orientation and A4/A3 by diagram size, fitted with margins under a title line (project name + date), JPEG-embedded (0.92) to keep files sane (Bouchard Findlayson: 444 KB vs 47 MB as PNG), saved as {project}_diagram.pdf~~
- ~~July 2026 audit — all Critical & High findings fixed (see audit-2026-07-09.md): arc-flash clearing time overhauled (source-directed BFS device search with cross-transformer current referral replaces the one-hop scan; gG fuse curves ported to the backend and evaluated at the arcing current instead of a fixed 0.02 s — up to ~100× energy understatement; relays resolved via trip_cb/associated_ct with real IDMT curve evaluation replacing dead heuristic code); DC arc flash now solves the Stokes & Oppenlander model iteratively (I^0.88 arc resistance — incident energy was understated ~2.7×); zero-sequence walker forwards the entry port through cables so Dyn transformers behind a cable no longer appear as phantom Z0 sources (earth-fault currents on NER systems); backend PDF reports relabeled IEEE 1584-2018 → 1584-2002 with printed equations matching the engine; TCC transformer damage curves select the IEEE C57.109 K per category (Cat II/III/IV anchored at 2 s at max through-fault instead of Cat I 1250); relay-tripped CBs substitute the relay on grading paths instead of vanishing; upstream 51N now graded against downstream phase devices at ik1; cable-R migration recognizes already-hot v1 files (no more ×1.275 re-inflation) and logs snap/scale/skip counts; every topology mutation (delete/cut/paste/new wire/bus resize) clears analysis results with the standard notice and undo/redo clears all 12 result slots; destructive keyboard shortcuts blocked while modals are open; stored-XSS closed at ~40 innerHTML sinks (SVG name labels, result modals, TCC panels, library tables incl. attribute contexts, breadcrumb, compliance report, voltage dialog) via shared escHtml; 19 new regression tests (backend/tests/test_arcflash_clearing.py, test_fault_dc_fixes.py) — 58 total pass~~
- ~~July 2026 audit — Medium & Low findings fixed (28 of 29; see audit-2026-07-09.md): utility Z_Q now includes the voltage factor c per IEC 60909-0 Eq. 15 so declared fault levels are reproduced exactly (regression expectation recomputed), with frontend worked examples matching; adiabatic checks use Ith = Ik″·√(m+n) (shared thermal_m_factor helper, also now reported per bus per the CLAUDE.md promise) and IEEE 80 decrement factor D_f applied to grid current; SLG/LLG calc modals display the true complex-sum impedance via new z2_mag/z_slg_mag backend fields (old magnitude-sum overstated Z_SLG ~20% on NER cases); motor x_pp display matches the engine convention; cable pu base in fault tracing uses bus-inferred voltage like load flow; unset ampacity reports "unknown"; 700 A validity floor; ACB short-time settings honored in arc-flash clearing; duty check breaks on Ib (with basis reported) and making factor is 2.5/2.6 by frequency (backend + compliance); TCC coordination test points capped at the pair's downstream through-fault, earth (ik1) points not referred across delta/zigzag windings, DFS path builders capped (MAX_PATHS/MAX_EXPANSIONS), focused grading includes CT/trip_cb-resolved relays, order-aware fuse CTI, autoCoordinate restores settings and reports uncoordinatable pairs (and syncs comp.props), fuse-fuse tiers 1.6:1 IEC minimum / 2:1 recommended, assumed mode excludes damage curves and applies the ratio rule, reverse-set 67s excluded from forward grading, ST/inst pickup labels corrected to ×Ir; compliance gains the IEC 60364-4-43 cable withstand section (t_clear vs k²S²/I² using real device curves) and walks through series device stacks; Paste Here snapshots final positions, port snapping is sheet-filtered, local revisions are per-project namespaced with clearLocal on every project switch (incl. templates) and restore clears undo, CSV exports quote newlines and guard formula injection (frontend + backend), scenarios snapshot/reconcile groups+pages (no more Infinity rects), bus resize anchors the fixed edge, read-only DB schedule close skips the commit ritual and Escape works in cells, report endpoints get the API timeout, nextId is clamped to the max loaded id, modal-wide/listener leaks fixed; backend save path preserves digit-string props byte-exact ("007" stays "007") via a props serializer while analysis still receives numbers; new tests: route-level round-trip suite (TestClient, temp DB), node-runnable migration idempotency test (frontend/tests/test_migration.mjs), duty/clearing anchors — 62 backend tests pass. Deferred: PROT-21 (re-fit generic gG fuse curve rows to the IEC 60269-1 0.1 s gate table) — requires verified standard gate data before changing curve values used for arc-flash energy~~
- ~~Lightning Risk Assessment (IEC 62305-2): `backend/analysis/lightning_risk.py` evaluates risk R1 (loss of human life) for a rectangular structure with up to power + telecom service lines — collection areas A_D/A_M (eq. A.2/A.7), dangerous events N_D/N_M/N_L/N_I with C_D/C_I/C_E/C_T factors, all eight risk components R_A–R_Z with the Annex B probability tables (P_B by LPS class, P_SPD/P_EB by SPD level, P_MS via K_S4 = 1/U_W, P_LI by line type & withstand) and Annex C losses (L_T/L_F/L_O with r_t, r_p, r_f, h_z). R_C/R_M/R_W/R_Z included only where internal-system failure endangers life (hospitals, explosion risk). A protection ladder (none → SPDs → LPS IV+SPD → … → LPS I + LPL I SPDs) reports R1 per option and recommends the minimum compliant set, or states that LPS+SPD alone cannot reach R_T = 1e-5 (with an additional-measures warning). Exposed at `POST /api/analysis/lightning-risk`; frontend modal in Studies menu with a persisted parameter form (`AppState.lightningRisk`, saved in project JSON), verdict banner, component-share table and ladder table. Simplifications documented in the engine docstring: single zone, no adjacent structure, K_S1 = K_S2 = 1, R2/R3/R4 out of scope. 9 hand-calculated regression tests (93 total pass); headless Playwright verified the form, run, rendering, and persistence.~~
- ~~Raceway / Conduit Fill & Grouping Derating: `backend/analysis/raceway.py` checks user-defined conduits with assigned project cables for (1) fill vs the NEC Chapter 9 Table 1 limits (53 % / 31 % / 40 % by cable count), (2) jam ratio 1.05·ID/OD for 3 similar cables with a 2.8–3.2 danger-band warning, and (3) ampacity grouping derating per IEC 60364-5-52 Table B.52.17 item 1 (between tabulated counts the next-higher entry applies — conservative). Cable ODs use an explicit `od_mm` prop when present, else a catalogue-typical 3/4-core SWA OD log-interpolated from the conductor size (flagged ≈ in results). Conduit internal diameters from typical heavy-duty sizes 20–160 mm, overridable. Exposed at `POST /api/analysis/raceway`; frontend "Raceway / Conduit Fill" modal in the Studies menu manages raceways (name, conduit size, ID override, cable checkboxes) persisted as `AppState.raceways` in the project JSON. Parallel-cable feeders (`num_parallel`) expand to one physical cable per run with the load-flow branch current split across runs; operating currents come from the latest load-flow `i_amps` when available and drive derated-ampacity pass/fail. 7 hand-calculated regression tests (100 total pass); headless Playwright verified the editor, run, rendering, parallel-run expansion, OD-estimate markers, and persistence.~~
- ~~Control Circuit / Schematic Diagram with live simulation: a new "Control Circuit" sidebar category adds eight IEC 60617 component types (`ctl_supply` 230/110 VAC + 24/110 VDC, momentary NO/NC pushbuttons, maintained selector switch with NO/NC contact type, coil-bound NO/NC auxiliary contacts, coil with contactor/relay/on-delay/off-delay types, pilot lamp with lens colours) drawn on the normal SLD canvas with the existing wire engine and ignored by all power-system analyses (`CONTROL_TYPES`). `frontend/js/control-sim.js` simulates the circuit live (Analysis → Control Simulation, Esc exits): every device is a two-terminal series element; a coil/lamp energizes iff its terminals reach L and N of the same control supply through wires and closed contacts (union-find nets, fixed-point iteration so seal-in circuits resolve, 30-iteration oscillation guard with a status warning). Contacts bind to coils by tag; timer coils update on a 200 ms tick (on-delay fires after continuous energization, off-delay holds after drop-out). In simulation, holding a pushbutton actuates it, clicking a selector toggles it (symbol redraws per state), energized coils/lamps and conducting contacts highlight green (lamp glass takes its lens colour), L-side live wires highlight orange; the simulation is fully transient — switch positions are restored on exit and nothing persists to the project. Control symbol classes added to symbols.css (light+dark) and to the export stylesheet in project.js. Headless Playwright verified a full DOL starter: start seals in via the aux contact, stop drops it out, on-delay timer fires only after its delay, switch state restores on exit, no console errors.~~
- ~~Wire overlap & routing correction UX: default-routed wires sharing a horizontal corridor (same 20 px midY bucket, overlapping x-spans) now fan out into ±10 px lanes automatically (interval-colouring in `Canvas._computeWireLanes`), so parallel branches — ubiquitous in control schematics — no longer render on top of each other. The mid-run of any default-routed orthogonal/spline wire is now directly draggable: hovering it shows an ns-resize cursor (set on the wire path element — its stylesheet cursor:pointer outranks svg-level styles), dragging moves the corridor (10 px snap), persists as `wire.midY` in the project JSON, participates in undo/redo, and the wire context menu gained "Reset Route" (clears midY + bend points; disabled when already auto-routed). Effective midY is used consistently for rendering, marquee hit-testing, and drag detection via `Canvas.wireMidY`. Headless Playwright verified lane separation, cursor affordance, drag+snap, persistence, undo, and Reset Route; the control-sim DOL suite re-passed on top.~~
- ~~Control circuit: animated contacts + MCB breaker: the moving piece of every contact-type symbol (pushbuttons, selector switch, bound contacts, breaker) is now a group pivoted at the bottom terminal whose rotation is class-driven (`.ctl-mv-no` rest-open / `.ctl-mv-nc` rest-closed, overridden by ControlSim's `.ctl-open`/`.ctl-closed` during simulation) with a 150 ms CSS transition — contacts visibly swing open/closed as the circuit operates. Switch/breaker toggles during simulation no longer call Canvas.render() (a DOM rebuild would kill the transition); the classes carry the visual and states still restore on exit. New `ctl_breaker` component (IEC 60617 contact with X marker, Q-tag, rating prop): a maintained conductor toggled by clicking in simulation, state saved/restored like selector switches. Rest-rotation classes added to the export stylesheet and verified applied during rasterization. Headless Playwright verified rest positions, transition property, breaker interrupting a lit lamp circuit, swing-out class flip without re-render, state restoration, and the full DOL suite re-passed.~~
- ~~DB circuit-schedule standing earth-leakage check: each way carries a `leakage_ma` field (new "Leak (mA)" column) pre-filled from `DB_LOAD_TYPES.leak_ma` — typical per-unit standing leakage from product-standard limits (IEC 60335-1 portable ≤ 0.75 mA / heating 0.75 mA per kW, IEC 62368-1 IT ≤ 3.5 mA, IEC 61851 EV ≤ 3.5 mA, IEC 61347 LED drivers ~0.4 mA) plus cable insulation leakage at 0.5 mA/100 m (`DB_CABLE_LEAK_MA_PER_M`). A live panel above the schedule sums standing leakage per EL group, each with an IΔn selector (10/30/100/300/500 mA persisted as `comp.props.el_ratings`, default 30, pruned on commit), and flags nuisance-trip risk when the total exceeds 30 % of IΔn per IEC 60364-5-53 §531.3.2; ungrouped (unprotected) ways report separately. Leak column round-trips through Excel export/import, keyboard nav and multi-cell paste; properties panel shows the worst group's standing/limit readout; the PDF DB schedule gains a Leak mA column and a per-group leakage summary with OK/EXCEEDED verdicts. Headless Playwright verified preset prefill, group totals, over/under-limit flagging, rating persistence, read-only-visit diff cleanliness, and stale-rating pruning; backend suite 100/100.~~
- ~~MCB support for the CB block: `cb_type` gains `'mcb'` alongside mccb/acb with an IEC 60898-1 curve-class select (B/C/D, shown only for MCBs); picking a curve — or switching a CB to MCB — sets `magnetic_pickup` to the top of the standard band (`MCB_CURVE_MAGNETIC`: B 3–5×→5, C 5–10×→10, D 10–20×→20×In, conservative for instantaneous-reach checks). 25 MCB entries added to `STANDARD_CBS` (B 6–63 A, C 6–125 A, D 16–63 A; Icn 6 kA ≤63 A / 10 kA 80–125 A per SANS 156) selectable from the Standard CB dropdown and the Settings CB library (type select gains MCB). TCC treats MCBs as thermal-magnetic via the existing `cbTripTime` model (20 ms magnetic like MCCB), carries `mcb_curve` through cbParams/addCustomCB, offers MCB in the custom-device type select, and labels curves/settings "MCB nA curve X"; cable-sizing backend clears MCB/MCCB instantaneous at 0.05 s (ACB 0.08 s); arc-flash/duty/PDF paths handle mcb via the existing non-ACB branches. Headless Playwright verified library values, trip-time regions, standard apply, curve↔pickup sync, conditional field show/hide, and TCC param plumbing; backend suite 100/100.~~
- ~~Hybrid inverters with battery backup + standalone BESS: Solar PV gains `inverter_type` (grid-tied/hybrid) — hybrid reveals a Battery Storage section (capacity kWh, usable DoD %, max charge/discharge kW, round-trip efficiency, SoC %, mode auto/charging/discharging/idle); new `battery` BESS source component (IEC battery-cell symbol, palette, voltage propagation, canvas label, calculated values incl. usable energy and autonomy at max discharge). Load flow (`loadflow.py`): unified battery pass resolves each unit before genset commitment and merit dispatch — charging adds bus load, explicit discharging injects at the limit (and can anchor/balance an island: grid-forming backup), auto self-consumption discharges into the island's renewable shortfall or charges from the surplus, with SoC gating above the DoD reserve floor and DC-coupled hybrid discharge capped by inverter headroom (rated − PV output); dispatch table gains charging/discharging entries and hybrid net flows accumulate on the shared component id. Fault (`fault.py`): `_battery_impedance` — inverter current-limited per IEC TR 60909-4 (fault_contribution_pu × Irated, X/R 10) across 3φ/SLG/LL/LLG paths, breaking current without decay. New Backup Autonomy study (`backup_autonomy.py`, POST /api/analysis/backup, Studies-menu button + results modal): removes utilities, islands the network (open CBs block), checks island kVA vs backup inverter capacity and kW vs battery discharge limits, reports autonomy hours night/with-PV (usable kWh × √η_rt ÷ net load; unbounded when PV covers the load) and flags unbacked islands. 13 regression tests added (113 total pass); headless Playwright verified palette/symbol/fields show-hide, calc values, and the study modal end-to-end (SECTION_ORDER fix: custom 'battery' section now renders).~~
- ~~Essential/non-essential load flag for the backup study: static loads, both motor types and distribution boards gain an `essential` select (yes/no, default yes so existing projects are unchanged) in the Load Flow section. The Backup Autonomy study assumes the changeover sheds anything flagged 'no': non-essential kW is excluded from the inverter-capacity, discharge-power and autonomy calculations, reported per island as `shed_kw` with an "excluded (assumed shed by the changeover)" note, and the modal labels the load column Essential Load (unbacked islands also list their shed load). Regression test pins an 8 kW essential + 4 kW non-essential island to the unchanged 1.07 h autonomy (114 total pass); headless Playwright verified the field default and the end-to-end shed accounting in the modal.~~
- ~~PV string/panel modelling for Solar PV (both hybrid and grid-tied): a PV Sizing Mode select (Rated kW AC — legacy default, unchanged behaviour — vs Strings × Panels DC) opens a "PV Array / DC Strings" section: panel module preset library (`PV_PANELS`: 330 W poly / 450–600 W mono/N-type with STC Voc/Vmp/Isc/Imp and β/γ temperature coefficients), panel W, panels per string, strings per inverter, inverter DC limits (MPPT count, MPPT min/max V, max DC input V, max input A per MPPT) and site temperatures (min ambient, max cell). Sizing level: derived DC kWp (strings × panels × W), DC/AC ratio with >1.5 oversizing warning, and load flow output = min(DC × irradiance, inverter nameplate) — an oversized array now clips at full sun (`_source_output_mva` array branch; hybrid battery headroom and the backup study inherit it automatically). Electrical level (IEC 62548): live calculated-value checks with ✓/✗ — string Voc at coldest ambient vs max DC input, string Vmp at hottest cell vs the MPPT window, 1.25×Isc × strings-per-MPPT vs the input current limit — plus a "PV DC String Design" section in the compliance report with pass/fail/warn items and remediation hints. The SLD symbol gains a panel-array glyph with the string layout (nS×mP) and the canvas label shows the kWp line and clipped output. 3 regression tests pin the clipping (117 total pass); headless Playwright verified mode switching, library apply, all three string checks (pass and fail paths), the compliance section, and the canvas glyph.~~
- ~~Clickable app title with hard refresh + version badge: the toolbar "ProtectionPro" title is now a click target (pointer cursor, hover underline, tooltip) that reloads the app via a cache-busted `?v=<timestamp>` URL replace so the browser re-fetches everything; the beforeunload guard still prompts on unsaved changes. A version badge renders below the title from the new `APP_VERSION` constant in constants.js, starting at V3.0. Headless Playwright verified the badge text, pointer affordance, cache-busted navigation, and clean reboot after refresh.~~
- ~~ⓘ tooltips for all PV inputs: 33 `FIELD_INFO` entries covering every Solar PV field — plant (rated kW, inverters, efficiency, PF, MPPT mode, irradiance, inverter type, fault contribution), the full PV Array / DC Strings section (sizing mode, panel library, panel W, panels/string, strings, Voc/Vmp/Isc/Imp, β/γ temperature coefficients, MPPT count/window, max DC voltage, max input current, site min-ambient and max-cell temperatures) and the hybrid battery parameters — each explaining what the value is, its datasheet source or IEC 62548 basis, and what the related check does; the 9 BESS component fields carry mirrored hints. Uses the existing prop-info-btn popup mechanism (no new UI code). Headless Playwright verified full coverage on a hybrid+array PV, popup content, and the BESS mirror.~~
- ~~Mobile: working ⓘ tooltips + touch move/select/wire: the mobile properties sheet copies the desktop panel via innerHTML, which silently dropped the info buttons' listeners — they are now re-bound in the sheet (popup capped at 100vw−24px and z-index above the sheets). Canvas touch handling gains entity interaction: a single-finger touch starting on a component/wire/port forwards synthetic mousedown/mousemove/mouseup through the desktop pipeline — tap selects, drag moves with grid snap, port drags draw wires, and a second finger commits the drag and switches to pinch-zoom; touches starting on empty canvas pan (fixing the long-broken one-finger pan whose mode guard compared against 'SELECT' instead of the lowercase MODE.SELECT value — previously only two-finger pan worked). Verified headlessly on an emulated iPhone 13: tap-select, snapped touch-drag (+100 px), empty-canvas pan, 9 ⓘ buttons in the sheet and a fitting popup.~~
- ~~Mobile tap-select fix (real devices): three genuine bugs found with real CDP touch injection after the synthetic-event tests passed falsely — (1) forwarded mousemove/mouseup targeted `document`, which has no `.closest()`, so Canvas.onMouseUp threw mid-gesture and selection (finalized on mouse-up) never landed; events now target `document.elementFromPoint` under the finger (also letting wires finish on the port they're dropped over); (2) touches starting on a port hit-circle silently started a wire (desktop select-mode convenience) — on touch in Select mode they now retarget to the parent component so taps select and drags move (Wire-mode FAB still draws from ports); (3) the selection re-render on the forwarded mousedown DETACHES the touched node, after which the remaining touch events fire only at the detached target and never bubble — the rest of the gesture is now tracked by listeners bound directly to the touchstart target, with a stuck-gesture safety in the next touchstart. Verified with real CDP touch on an emulated iPhone 13: tap on the port circle selects, drag moves +110 px snapped, gesture state clears, and empty-canvas pan still works afterwards.~~
- ~~Mobile header version badge + tap-to-refresh: the mobile header title becomes a stacked title/version block showing `APP_VERSION` (V3.0) below "ProtectionPro", and tapping it runs the same cache-busted hard refresh as the desktop title — giving phones (where the desktop toolbar is hidden) a way to force-fetch a new deploy. Version text and refresh binding are shared with the desktop block in app.js (single APP_VERSION source). Verified headlessly on mobile and desktop viewports.~~
- ~~Mobile touch interaction rebuilt on native Pointer Events: the synthetic mouse-event forwarding layer in mobile.js is gone — Canvas's own handlers now bind `pointerdown`/`pointermove`/`pointerup`/`pointercancel` (PointerEvent extends MouseEvent, so the entire desktop select/drag/wire/marquee code runs unchanged for mouse) with touch routed at the top of each handler: single-finger touch on an entity runs the exact desktop select+drag path (with `svg.setPointerCapture` so a mid-drag re-render can never strand the gesture on a detached node), touch on empty canvas pans, a stationary (<10 px) empty-canvas tap deselects, a second finger commits the in-flight gesture and switches to pinch-zoom (two-finger pan follows the midpoint; one finger lifting resumes as pan). Touch works at EVERY viewport width — the old layer was gated on the 768 px phone layout, leaving iPads and landscape phones with no touch at all. Fat-finger rules: in Select mode a touch on a port acts on its component (Wire mode draws from ports), and data/name labels are mouse-only drag targets (a finger on a label selects/moves the component — previously it silently started a label nudge). mobile.js is now purely the phone UI (sheets/nav/FABs/selection bar); control-sim's pushbutton listeners converted to pointer events so simulated controls work by touch. Fixed alongside: the phone selection bar no longer resizes the canvas container when it appears (selection changes on pointer-down; the 40 px reflow mid-gesture corrupted every touch-drag's coordinate mapping — it overlays instead), the minimap no longer hijacks drags that merely cross it (drag-to-scrub only engages when started on the minimap; previously it fought Canvas's document-level drag in a runaway feedback loop), the Wire FAB active-state check compared against `'WIRE'` instead of `MODE.WIRE`, and `touch-action: none` on `#sld-canvas` applies at every width. Verified with a 27-check headless suite using real CDP touch injection against the deployed container at 390×844 (phone), 900×700 (tablet) and 1280×800 (desktop mouse regression): tap select, tap-empty deselect, exact-delta snapped drags, pan (selection preserved), pinch zoom, wire-mode port→port touch drag, select-mode port tap safety, selection bar, marquee/middle-pan/wheel/wire-drag desktop parity, zero page errors.~~

### Plan Markup review fixes (three-stage UX/EE/senior-dev review → implementation)
- ~~**Sub-boards now participate in analysis (EE-1, critical):** a `distribution_board` is modelled as a bus-like node (busbar carrying its own lumped load AND passing current in→out to any sub-board it feeds) across loadflow, unbalanced loadflow, fault, cable-sizing and arc-flash. Previously a board blocked every network walk, so a plan-synced "Feeder to Sub-board" produced a diagram that drew correctly but analysed wrongly — the sub-board's demand vanished, its outgoing bus islanded, its feeder carried zero current, and no fault level existed downstream of any board. New `TestPlanSubBoardFeeder` regression (3 tests) pins the plan-sync topology: sub-board energised/supplied (balanced + unbalanced), utility supplies MDB+SDB, feeder carries the sub-board current, fault level computed at the sub-board. 179 backend tests pass.~~
- ~~**Circuit-schedule integrity (EE-7, F-PIN, EE-3, EE-14):** ways carry a stable internal id (device tags key on it, way number is a free display field) so renumbering/duplicate numbers can't remap a device's load; hand-edited loads/lengths/descriptions/poles on plan-driven ways are now pinned (📌/🔄 indicator + unpin) instead of being silently overwritten by the next sync; deleting or re-tagging all devices on a way zeroes/flags the orphaned way instead of leaving a ghost load; sync only escalates 1P→3P and never silently demotes a 3-phase way to R.~~
- ~~**Plan feeder cables carry real electrical data (EE-4/6/9):** feeder cable type resolves against the building-cable library (r/x/rating/size, 0.4 kV) instead of keeping 11 kV/400 A palette defaults; feeder length/name are pin-protected against auto-reflected straight-line chords; the "Feeder to Sub-board" way derives its size and shows a downstream-demand overload warning. Feeder direction is inferred from the SLD supply path (EE-5) rather than route draw order.~~
- ~~**Cross-view undo safety (UX-4/SD-1):** sync/bulk-assign/auto-tag push paired undo snapshots and heal undo-stranded links so an SLD-side undo no longer escalates into a prompt to delete the user's plan boards.~~
- ~~**Plan workspace UX:** floating ✓ Done / ✕ Cancel chip so multi-point tools (route/trench/measure/room/SL-path/device-path) can be finished on touch; undo/redo buttons + a live mode/status line + an always-visible calibration chip on the plan toolbar; link 🔗 badges on linked plan & SLD entities (plus a "Linked to the distribution plan" note in the SLD properties panel); tap opens the mobile props drawer only on a stationary tap (not mid-drag); `V` select shortcut + a Plan-workspace shortcuts table in Help; busy state on DXF/PDF round-trips; measurement properties panel; per-change (not per-keystroke) snapshots; ≥32 px touch targets; labelled "Plan type" selector; accessible floor-manager modal; focusable plan canvas with arrow-key selection; amber warning-toast styling. Class-aware new-way presets, active-floor auto-tagging, DD/DXF import fidelity, all-1P board current at 230 V, and assorted nits.~~
- ~~**Mobile element-properties select fix:** the searchable cable-size dropdown committed selections on `mousedown`, which touch browsers withhold over a scrollable list — so cable sizes could not be selected on mobile. Switched selection to `click` and outside-dismiss to `pointerdown`. Verified end-to-end headless (touch): tapping a cable option sets `standard_type`.~~
